# 《艾尔登法环》互动地图 Online-first V1 执行方案

> 文档版本：1.0
> 编制日期：2026-08-17（Asia/Shanghai）
> 目标：优先使用可合法接入、可追溯的在线数据，在不接触前台游戏的情况下尽快交付真实可用 V1；本地游戏文件用于后续校准、补全和 1:1 验收。

## 0. V1 定位

V1 是“在线数据驱动的真实可用地图”，不是演示数据，也不等待完整本地归档提取。

V1 必须做到：

- 数据来自真实在线 Wiki、开源数据库和人工路线资料，每条记录保留来源、revision/commit、许可证和获取时间；
- 覆盖交界地地表、地下、独立地图/楼层和幽影之地四类顶层空间，并把玛利喀斯前王城与灰烬王城建成独立 `WorldEpoch`；
- 优先覆盖全部成就依赖、全部赐福、Boss、关键物品、传送/电梯/梯子/跳落等导航节点；
- 路线按“已知赐福 → 入口地标 → 楼层 → 必经连接 → 条件/单向边 → 目标”输出；
- 世界状态至少区分玛利喀斯前王城与灰烬王城；
- 不生成任何无来源的节点、坐标或连接；
- 在线数据冲突时显示证据等级和差异，不静默选一个；
- V1 页面明确标注 `Online Verified V1`，不得提前宣传为“游戏文件 1:1”。

V2 再使用本地安装文件、MSB、Param、FMG、EMEVD、碰撞和 Navmesh 对 V1 做坐标、ID、楼层、状态和完整拓扑校准。

## 1. 在线来源分层

### 1.1 可直接结构化接入

| 来源 | V1 用途 | 接入方式 | 许可/限制 |
|---|---|---|---|
| Eldenpedia (`eldenring.wiki.gg`) | 地区、物品、Boss、NPC、任务、获取路线、楼层和入口语义 | MediaWiki Action/REST API；保存 pageid、revision、URL、抓取时间 | 页面文本通常为 CC BY-SA 4.0，逐页检查例外；媒体逐文件核权，V1 默认不导入图片 |
| ERDB | 物品、参数化属性、版本化 ID 和基础数据库 | 公共 REST API 或固定 Git commit 的 JSON/Schema | MIT；数据版本和更新时间必须实测，不能假设当前 API 已覆盖本机 2.6.2.0 |
| FrontierNav | 实体、语义关系、地区归属和知识图谱查询 | 公开页面/Deep Query 导出；保存关系类型和 revision | 文本通常 CC BY-SA 4.0；非文本媒体另行核权；语义关系不得直接变成通行边 |
| Map For Goblins 开源仓库 | POI 分类、约 6700 vanilla+DLC 标记的覆盖规模、提取规则和候选数据 | 固定 commit；只使用逐文件确认受许可证覆盖的源码/结构化数据，不运行 DLL | MIT-style；接入仍为条件式，排除运行时注入、Overlay、内存数据和未明确授权的游戏资产 |

### 1.2 条件导入

| 来源 | 可用部分 | 不可直接使用部分 | 处理方式 |
|---|---|---|---|
| `aether-auto/er-guide` | `regions → legs → steps` 路线结构、赐福到赐福顺序、全收集覆盖工作流 | Fextralife 瓦片、来源混合的地图坐标和图片 | 路线文本/结构按文件级许可证与来源逐项审计；瓦片不导入；坐标进入 provisional，至少再找一个独立来源确认 |
| Elden Ring Compass | 当前数据模型、提取器设计、只读进度字段和预构建数据结构 | 许可证尚未完成根级审计的代码/数据、游戏图标 | 先做 Schema/统计对照；未确认许可证前不复制实现或资产 |
| Elden Ring Fan API | 基础物品描述和分类补充 | 仓库当前未见清晰根许可证、数据源含抓取内容 | 只作差异触发器；许可证确认前不进入发布数据 |

### 1.3 只作人工核对

| 来源 | 用途 | 禁止事项 |
|---|---|---|
| MapGenie | 点位查漏、常用分类和用户措辞 | 不绕过 robots、不抓私有接口、不镜像瓦片、不批量复制坐标 |
| Fextralife | 区域 walkthrough、入口文字和路线图人工复核 | 不镜像瓦片、不抓内部 API、不复制受保护攻略正文 |
| Wand | 实时位置、附近点和清单 UX 对标 | 不抓位置服务或专有点位数据 |
| AtlasForge | 地表/地下/幽影之地分层和 3D UX 对标 | 不复制瓦片、坐标或专有数据库 |
| IGN/Game8/其他攻略 | 第二来源、特殊路线和中文别名核对 | 不作为批量主源，不覆盖更高等级来源 |

## 2. V1 数据产品

V1 发布四套相互关联的数据：

1. `entities`：赐福、地区、Boss、NPC、物品、入口和导航设施；
2. `placements`：实体在来源坐标系中的位置、图层、区域、楼层和证据；
3. `route_legs`：赐福到赐福或赐福到目标的人工验证路线段；
4. `relations`：语义关系、候选通行关系和已验证通行边，三者硬隔离。

每条记录至少包含：

- `canonical_id`；
- 多语言名称和别名；
- `source_id/source_url/source_revision/source_license/fetched_at`；
- `source_coordinate_system` 和原始坐标；
- 顶层位面、区域、楼层和世界纪元；
- `evidence_grade`；
- `verification_state = online_single / online_cross_checked / local_game_verified`；
- 冲突列表和最后复核时间。

## 3. 坐标与底图策略

V1 不等待游戏文件生成底图，但也不盗用专有瓦片：

- 采用自制的区域轮廓、道路骨架、楼层示意和抽象底图；
- 所有在线坐标保留原始来源坐标系，不直接冒充游戏世界 X/Y/Z；
- 对同一图层选取赐福、入口、神授塔等控制点建立版本化二维变换；
- 只有两个独立来源一致或经过人工截图核对的点，才进入默认显示；
- 坐标冲突超过阈值时展示 warning，不用平均值掩盖错误；
- 后续本地 MSB 坐标到位后，V1 坐标通过 migration table 转成正式坐标，不改 canonical ID。

V1 可以“真实可用”，但未完成本地坐标校准的点必须显示 `community-coordinate`，不能显示“游戏原始坐标”。

## 4. 路线构建

V1 路线不靠二维距离自动生成。

来源顺序：

1. `er-guide` 的 grace-to-grace legs 提供候选顺序；
2. Eldenpedia/Fextralife/其他攻略的入口和楼层描述补充连接语义；
3. 至少两个来源一致后，候选边晋升为 `online_cross_checked`；
4. 单向跳落、电梯、梯子、传送、棺材、Boss 门和状态锁必须单独建边；
5. FrontierNav 的 `located_in/MapFeature-MAP_TARGET` 等关系永远只进 SemanticRelation；
6. 无法确认方向或条件的连接保留为候选，不参与默认寻路。

V1 默认提供“策划过的真实路线”，而不是宣称任意两点之间都能自动算出完整最短路。随着经过交叉验证的 Transition 增加，再逐区开放自动寻路。

## 5. V1 优先覆盖顺序

### P0：直接解决补档和成就

- 全部成就 Boss；
- 全部传说武器、护符、魔法/祷告、骨灰；
- 全部结局和关键任务分支；
- 易错过目标，尤其古兰桑克斯的雷电；
- 全部地图碎片、关键赐福和进入 DLC 的条件；
- 玛利喀斯前/后王城状态。

### P1：完整导航骨架

- 全部赐福；
- 所有大型遗迹、地牢和小型地下城入口；
- 井、电梯、梯子、棺材、传送、气流和单向落点；
- 地表/地下/幽影之地/独立地图之间的连接；
- 高风险多层区域的入口地标和楼层说明。

### P2：全收集点位

- 唯一武器、防具、法术、祷告、战灰、骨灰和关键物；
- 强化资源和成长物；
- NPC、商人、入侵、召唤和任务节点；
- 可重复素材默认折叠。

## 6. 游戏运行期间的在线同步

在线 V1 属于 `GAMING_SAFE + ONLINE_INGEST`，必须满足：

- 只请求文本/JSON/小型仓库文件；不下载瓦片、图片、视频或整仓库历史；
- 单响应默认不超过 8 MiB，单任务累计不超过 32 MiB；
- API 请求平均不高于 1 次/秒，遵守来源的 `Retry-After`、robots 和 API etiquette；
- 使用明确 User-Agent，记录响应 ETag、Last-Modified、revision/commit 和 SHA-256；
- 网络限速不高于 1 MiB/s，避免影响在线游戏；
- 每次同步写入 source snapshot，规范数据只从 snapshot 构建，不能直接依赖在线实时响应；
- 来源不可用时使用最后一次已验证 snapshot，并显示数据日期，不切换到无来源镜像；
- 不启动浏览器 GUI、UAC、安装器或任何前台窗口。

## 7. 在线数据冲突规则

| 冲突 | V1 处理 |
|---|---|
| 名称不同 | 官方简中 FMG 尚未接入时，以多数高等级来源为主名，其他作别名；标记待本地校准 |
| 坐标不同 | 不平均；保留全部来源坐标，人工选定并记录理由 |
| 路线不同 | 能复现且入口/楼层描述更完整者优先；另一条保留为备选 |
| 世界状态不同 | 分成独立 WorldEpoch，不叠成一条路线 |
| 点位只有一个来源 | 显示为 provisional，默认寻路不经过它 |
| 许可不清楚 | 只保存来源 URL/哈希和差异结论，不发布原文、图片或坐标批量数据 |

## 8. V1 验收门

V1 必须同时通过：

- 演示 `graph.json` 正式数据引用数为 0；
- P0 全成就依赖 100% 有来源、状态、最近赐福和可执行路线；
- 地表、地下、独立地图/楼层和幽影之地四类空间均可检索与切换，灰烬王城作为独立世界纪元显示；
- 所有默认路线边至少两个独立在线来源一致，或一个结构化来源加人工实测确认；
- 关键单向/跨层/传送连接方向明确；
- 任何 SemanticRelation 都不能参与路由；
- 每个点位显示来源、获取时间、许可、证据等级和坐标类型；
- 不可达目标返回阻断原因，不生成伪路线；
- “黄金树大教堂 → 古兰桑克斯的雷电”在玛利喀斯前可执行、灰烬王城明确不可获取；
- 不包含未经许可的在线地图瓦片或批量复制的专有坐标；
- 桌面端实际完成“搜索目标 → 查看楼层/入口 → 生成步骤 → 勾选完成”流程。

## 9. 交付节奏

| 里程碑 | 参考时间 | 结果 |
|---|---:|---|
| V1-A 来源快照 | 1–2 天 | 来源注册表、许可证、revision、首批在线 snapshot |
| V1-B 成就导航 | 3–7 天 | 全成就目标、关键状态、赐福到目标路线可用 |
| V1-C 导航骨架 | 1–2 周 | 四大图层、全部赐福/入口/主要连接和搜索可用 |
| V1-D 全收集扩展 | 2–4 周 | 唯一物、NPC、任务和主要收集点持续补齐 |
| V2 本地真值校准 | 后续并行 | 游戏原始坐标、完整楼层、全状态拓扑和 1:1 验收 |

## 10. 当前立即执行顺序

1. 冻结 `data/online-source-registry.json`；
2. 为每个可直接接入源获取最小 snapshot 和 revision；
3. 建立 canonical ID、来源、证据和坐标系 Schema；
4. 先导入全部成就依赖、赐福、地图区域和关键世界状态；
5. 导入并审计 `er-guide` 路线 legs，排除其瓦片和未清晰授权坐标；
6. 对 P0 路线做双源核对；
7. 接入现有 WebUI，彻底移除正式构建对 demo graph 的依赖；
8. 发布 `Online Verified V1`；
9. 本地归档可用后逐项升级为 `local_game_verified`，而不是推翻 V1 重做。

---

**最终原则：V1 先解决“现在能不能真实找到、从哪里进去、当前状态能不能拿到”；V2 再解决“每个坐标和每条边是否达到游戏文件级 1:1”。在线优先是交付顺序变化，不是降低来源、许可和真实性标准。**
