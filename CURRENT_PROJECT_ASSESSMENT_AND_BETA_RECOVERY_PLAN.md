# 《艾尔登法环》拓扑地图项目现状评估与 Beta 抢救计划

评估日期：2026-08-18  
评估角色：Sol  
评估对象：当前版本库、在线正式图、本地游戏数据副本生成物、路线网页和本地服务  
最高优先级：尽快交付真正能查询和解释路线的拓扑 Beta，而不是继续扩张底层物理研究

## 0. 术语与缩写约定

- V1(第一版, Version 1, 当前以固定在线来源建立的第一版正式图数据)。
- JSON(JavaScript 对象表示法, JavaScript Object Notation, 本项目存储和交换结构化图数据的文本格式)。
- UI(用户界面, User Interface, 玩家实际选择起终点并查看路线的页面)。
- API(应用程序编程接口, Application Programming Interface, 网页从本地服务读取图和目录数据的接口)。
- POI(兴趣点, Point of Interest, 地图上可搜索、定位或作为路线目标的具体对象)。
- DOM(文档对象模型, Document Object Model, 浏览器中全部页面元素组成的结构树)。
- MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring, 记录地图实体、区域、机关和显式跨图声明的地图文件)。
- EMEVD(事件脚本文件, Event Script, 社区沿用的格式名；记录事件指令、传送、条件和状态变化，并无可靠的官方逐字母展开)。
- NVA(导航网格布置文件, Navmesh Arrangement, 记录原生导航区域及连接声明的高层文件；英文展开是功能性称呼，不是已确认的官方逐字母释义)。
- NVC(导航连接文件, Navigation Connection, 本项目暂按功能命名的跨地图连接候选格式；官方逐字母释义尚未确认)。
- HKX(Havok 数据文件, Havok Data File, 保存导航网格或碰撞几何等底层数据；这里使用的是文件类别名，不主张它存在官方逐字母展开)。
- ObjAct(对象交互, Object Action, 门、升降梯、机关等可触发对象的交互记录)。
- E2E(端到端, End-to-End, 从打开页面、输入起终点到得到路线的完整用户流程)。
- QA(质量保证, Quality Assurance, 对数据、功能和真实用户流程进行验收)。
- DLC(可下载内容, Downloadable Content, 本项目中的《黄金树幽影》扩展区域内容)。
- XYZ(三维坐标轴, X-Y-Z Axes, 游戏空间中的三个方向坐标)。

本文不使用数学公式，因此不存在未解释的公式字母或特殊符号。

## 1. 执行结论

### 1.1 用户对产品目标的判断是正确的

这个项目真正需要的是“带条件的有向拓扑图”，不是连续步行模拟，也不是角色碰撞物理模拟。

对玩家有价值的路线只需回答：

- 从哪个赐福或入口出发；
- 依次经过哪些关键节点；
- 使用门、梯子、升降梯、传送门、棺材、跳落还是普通通道；
- 哪些连接是单向的；
- 哪些连接受 Boss、任务、钥匙或世界状态约束；
- 目标位于地表、地下、遗迹内部、屋顶还是其他语义楼层；
- 当前条件不足时，到底缺哪一个直接相关条件。

它不需要回答角色在每一个导航多边形上如何连续移动，也不需要证明每一厘米碰撞面都可行。后者的成本远高于真实产品价值，而且无法替代入口说明、楼层语义和状态条件。

### 1.2 当前并非“什么都没有”，而是“产品主线被研究主线压住了”

当前已经存在一套接近可交付的在线正式图：

- 746 个正式节点；
- 1195 条有向路线边；
- 412 个赐福节点；
- 地表、地下、遗迹或地牢三类语义层；
- 381 条带条件的边；
- 178 个不同的状态条件；
- 全图只有一个弱连通分量，没有孤立节点，也没有失效的边端点；
- 每条边均具备通行方式、路线备注和来源证据；
- 在线覆盖审计中，42 项成就里有 40 项存在目标映射，并且这 40 项在“全部已登记条件满足”的覆盖假设下都有赐福到目标的路径；其余 2 项本身没有单一空间目标。

正式图见 [data/v1/graph.json](./data/v1/graph.json)。它已经足以支撑一个诚实标注覆盖边界的拓扑 Beta。

同时，当前又生成了一个完全不同性质的本地考据图：

- 29144 个节点；
- 7976 条拓扑候选边；
- 16314 条语义关系；
- 全部 7976 条边都被强制标为不可路由；
- 全部节点和语义关系也都被强制标为不可路由。

该图见 [data/v1/entities/local-abstract-topology-graph.json](./data/v1/entities/local-abstract-topology-graph.json)。它是证据仓库，不是玩家路线图。把它继续当作 Beta 的前置条件，只会不断增加数据量，不会让玩家更早得到路线。

### 1.3 Beta 距离很近，最终一比一拓扑仍然很远

在不继续扩张本地底层研究的前提下，一个真实可用、明确标注“尚非完整一比一”的拓扑 Beta，预计需要一个专注工程日，约 7 至 10 小时。

最终一比一拓扑不能诚实地给出完成百分比，因为项目目前没有建立“全量应有节点、应有边、状态版本和语义楼层”的固定分母。现有正式图覆盖大量代表性路线和成就目标，但尚不能证明覆盖了每个可玩位面、入口、屋顶、升降梯端点、单向落差和世界状态版本。完整目标至少是独立的多周工程，不能再作为 Beta 发布门槛。

## 2. 本次评估实际验证了什么

本次没有读取正在运行的游戏、进程内存或存档。测试对象仅为版本库、本地游戏副本生成物和临时启动的回环本地服务。

### 2.1 静态和数据验证

- [app.js](./app.js) 通过 JavaScript 语法解析。
- [server.py](./server.py) 通过 Python 语法编译。
- 在线正式图的全部边均指向存在的节点。
- 在线正式图只有一个弱连通分量，且没有孤立节点。
- 节点显示名无缺失。
- 边的通行方式、说明和来源证据无缺失。
- 在线覆盖审计脚本执行成功。
- 本地考据图审计脚本执行成功，并再次确认全部候选都不可路由。

### 2.2 本地服务验证

对首页、脚本、样式和首屏初始化相关数据共 21 项进行了回环请求测试：全部成功，没有失败项，总响应体约 638 万字节。

其中约 345 万字节来自本地地图和事件考据索引。这些数据对“先算出一条在线拓扑路线”不是必需的，却被当前页面初始化设为强制依赖。

### 2.3 真实页面验证

页面能够打开，路线算法能够工作，浏览器日志中没有项目自身产生的错误。

把起点改为“黄金树大教堂”后，页面成功生成以下 5 段路线：

1. 黄金树大教堂，经西侧出口或下楼梯，到大教堂西侧出口；
2. 乘电梯下降到西侧电梯下端；
3. 经下层楼梯和栏杆到古兰桑克斯上方栏杆；
4. 从栏杆单向跳落到巨矛下方平台；
5. 跳上巨龙石矛，取得古兰桑克斯的雷电。

这正是项目最初需要交付的产品形态，也证明路线引擎本身不是主要阻塞点。

### 2.4 代表路线的无界面回归结果

在相应条件满足时，以下路线均成功求解：

| 路线 | 路线段数 | 关键通行语义 |
|---|---:|---|
| 史东薇尔正门到“接肢”葛瑞克 | 5 | 遗迹内部、垂直路线、Boss 接近 |
| 火山官邸入口到拉卡德 | 5 | 隐藏路线、垂直路线、Boss 完成状态、传送 |
| 法姆·亚兹拉迎风露台到“黑剑”玛利喀斯 | 8 | 浮空岩、内部路线、Boss 状态、屋顶路线 |
| 圣树树冠到玛莲妮亚 | 9 | 垂直路线、屋顶、升降梯、腐败树根路线 |
| 希芙拉河井底到龙人士兵 | 5 | 地下通道、升降梯、倒塌石柱、传送门 |

这些结果进一步说明：当前缺的是可用产品收口，不是继续解析数千万条几何边。

### 2.5 已发现的直接产品缺陷

最重要的缺陷不是底层数据缺失，而是首个用户流程就配置错误：

- 页面默认目标是“古兰桑克斯的雷电”；
- 页面默认起点却是“大道旁露台”；
- 默认条件不包含击败王城金色幻影 Boss 的状态；
- 因而首屏直接显示“此刻不可达”；
- 阻断提示列出多个与这条局部路线无关的 Boss 条件，而不是指出“应从黄金树大教堂出发”或“尚未击败必要 Boss”。

也就是说，最能证明项目价值的示例路线实际上已经存在，但默认入口把它隐藏成了失败案例。

## 3. 为什么做了约 24 小时仍没有“大体能用”的 Beta

### 3.1 把“最终一比一”错误地解释成了“物理连续可行性”

“最终一比一”本来应当约束拓扑覆盖：节点是否齐全、边是否有向、条件是否正确、楼层是否清楚、世界状态是否区分。

执行中却逐渐把它解释成：必须解码底层导航几何、核验连接面的索引、证明连续步行、绑定每一个原生导航实例。这个解释把产品从“知识图谱和路线规划器”变成了“半个游戏导航运行时的离线重建”。二者预算并不在同一数量级。

### 3.2 证据晋级门槛被设成了“非完美即不可用”

本地考据链中的每条边默认不可路由，只有在端点、方向、状态、楼层和玩家空间全部精确绑定后才考虑晋级。这个策略适合最终取证层，却不适合在线优先的 Beta。

更合理的产品分级应当是：

- 已由多个在线攻略交叉核对，可用于路线；
- 已由游戏文件明确声明，可用于路线；
- 有高可信候选，但需显示警告；
- 仅为证据关系，不参与路线。

当前执行把前三类压缩成了最后一类，因此解析越多，玩家可走的边仍然是零。

### 3.3 研究数据被接进了首屏关键路径

当前 [app.js](./app.js) 在初始化时并行请求正式图、成就目录、路线目录、在线地图索引、本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 索引和本地 EMEVD(事件脚本文件, Event Script) 索引等 18 项数据，并把多数请求失败视为整个页面启动失败。

用于查路线的核心实际上只需要正式图和路线配置。坐标证据、在线大目录、本地考据索引和研究面板都应按需加载，任何一个缺失都不应阻止路线规划器启动。

### 3.4 页面把研究控制台和玩家产品混在了一起

实测首屏生成：

- 11777 个 DOM(文档对象模型, Document Object Model) 元素；
- 4002 个下拉选项；
- 起点和终点各 798 个选项；
- 在线地图选择 1037 个选项；
- 本地地图选择 1347 个选项；
- 图层中同时渲染约 798 个节点元素和 1576 个边元素。

这不是“数据不够”，而是“没有给普通玩家一个小而明确的入口”。玩家需要的是搜索起点、搜索目标、勾选少量状态、得到路线；不是在 4002 个下拉选项和本地文件检查器之间判断该点哪个数据源。

### 3.5 缺少以玩家任务为核心的验收顺序

如果第一条固定验收就是“黄金树大教堂到古兰桑克斯的雷电”，默认配置错误会在最早阶段暴露。当前大量审计验证的是解析字段完整、哈希一致和候选未被错误晋级，却没有把“用户打开页面后是否在一分钟内得到一条有用路线”设为第一发布门槛。

### 3.6 版本库没有形成可发布检查点

当前工作区有：

- 7 个已跟踪文件被修改；
- 74 个未跟踪文件；
- 249 个已跟踪文件；
- `data` 目录约 7.42 亿字节；
- `data/v1/entities` 下 34 个实体 JSON(JavaScript 对象表示法, JavaScript Object Notation) 文件合计约 7.37 亿字节；
- `scripts` 目录有 53 个当前脚本源文件；
- 被忽略的运行时工具目录约 970 万字节。

版本库原始游戏文件隔离做得正确，但“产品代码、轻量正式数据、重型研究生成物、研究工具和运行时产物”没有彻底分道。结果是无法明确指出哪一个提交就是 Beta，也很难做干净部署和回滚。

### 3.7 对先前执行流的客观评价

从生成物看，先前执行流并非没有工作，而是在错误的完成定义上高强度产出：大量底层数据已被成功解析、交叉绑定和审计。问题不在于产出数量，而在于这些产出几乎都被设计为不可路由，对用户首个任务没有形成增量价值。

因此根因是产品排序、验收标准和停止规则失控，而不是单个解析器是否足够复杂。继续沿原方向堆数据只会放大偏差。

## 4. 各类本地游戏数据究竟有什么价值

### 4.1 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring)

高价值信息包括：

- 地图编号和地图之间的显式目标声明；
- 门、升降梯、机关、碰撞部件、区域和入口实体；
- 部件的游戏空间位置；
- 某些连接对象的两端或目标地图；
- 可用于把在线攻略中的地点名称绑定到游戏原生实体。

它非常适合证明“这个入口属于哪张地图”“这个机关或连接对象实际存在”。它不必先绑定到每一个导航网格实例，才能支持抽象拓扑。

结论：保留并继续做有明确产品用途的定点绑定，但不再要求全量实例级精确绑定后才发布 Beta。

### 4.2 EMEVD(事件脚本文件, Event Script)

高价值信息包括：

- 精确传送指令；
- 事件触发前提；
- Boss 击败、任务旗标和世界状态变化；
- 门、升降梯和 ObjAct(对象交互, Object Action) 的启用或禁用；
- 王城前后状态、剧情传送和特殊进入方式。

它最适合补“条件”和“非步行传送”。但完整解释全部事件脚本需要运行时状态、保存数据和玩家上下文，静态分析无法可靠求出所有条件的当前真值。

结论：只提取与明确拓扑边直接相关的传送和条件，不做完整事件虚拟机，不以“静态求出所有运行时真值”为 Beta 门槛。

### 4.3 NVA(导航网格布置文件, Navmesh Arrangement)

当前已经解析：

- 997 个文件；
- 9480 个原生导航区域节点；
- 5884 个连接声明；
- 7201 个无向原生连通候选。

这些信息能证明同一地图内部某些原生导航区域存在连接关系，对最终拓扑覆盖审计有辅助价值。但它并不天然告诉我们：玩家该从哪个门进入、连接是上楼还是下楼、是否单向、是否被 Boss 门锁住、该区域应显示为地下几层。

结论：现有抽象连接图已经足够作为研究证据，立即冻结扩张；不把它作为 Beta 依赖。

### 4.4 NVC(导航连接文件, Navigation Connection)

当前已经从游戏副本中提取 3674 个候选文件，文件名直接带两个地图编号，因此它可能比底层几何更直接地提供跨地图连接候选。

但二进制字段语义尚未完成验证。它的价值取决于能否在很短时间内稳定输出“源地图、目标地图、连接类型或端点引用”。

结论：放到 Beta 之后做一次严格限时的可行性冲刺。两到四小时内若不能稳定产生可审计的跨图连接记录，就归档停止，不再无期限逆向。

### 4.5 HKX(Havok 数据文件, Havok Data File)

当前已解码的底层数据包括：

- 3390 个导航网格条目；
- 16607263 个顶点；
- 6888218 个面；
- 29901878 条几何边；
- 137358 组连接边界见证；
- 其中还有 9824 组索引空间冲突需要进一步解释。

这批数据对“连续物理可行性、精确边界和导航多边形”有价值，对“从哪个赐福经过哪个升降梯到哪个 Boss”价值极低。即使把全部冲突解决，也不会自动得到门的名称、路线方向、任务条件或玩家可理解的楼层。

结论：立即停止继续解码、扩张和冲突消解。保留现有产物和复现记录，不删除，但它们不得进入产品关键路径。

## 5. 技术方向取舍

| 方向 | 当前状态 | 用户价值 | 剩余成本 | 决策 |
|---|---|---|---|---|
| 在线正式拓扑图和路线引擎 | 已能跑代表路线 | 极高 | 低 | 立即推进到 Beta |
| 起终点搜索、条件输入和路线说明 | 已有功能但入口臃肿 | 极高 | 低到中 | 立即重构为玩家主流程 |
| 在线 POI(兴趣点, Point of Interest) 搜索和固定快照 | 数据丰富 | 高 | 低 | 保留，按需加载，不阻塞路线 |
| MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 显式地图声明 | 已大量提取 | 中到高 | 中 | 只做定点晋级和最终拓扑校验 |
| EMEVD(事件脚本文件, Event Script) 精确传送和条件 | 已有 15 条精确脚本传送及更多候选 | 中到高 | 中 | Beta 后定点推进，不做全解释器 |
| NVC(导航连接文件, Navigation Connection) 跨图候选 | 文件已提取，格式未完成 | 潜在中到高 | 不确定 | Beta 后限时两到四小时验证 |
| NVA(导航网格布置文件, Navmesh Arrangement) 抽象连接 | 核心提取已完成 | 中 | 继续扩张成本高 | 冻结，作为研究证据 |
| HKX(Havok 数据文件, Havok Data File) 几何和连续步行 | 已产生巨量结果 | 对本产品低 | 极高 | 立即停止，保留归档 |
| 全量导航实例与地图端点一一绑定 | 1125 个端点中 1103 个歧义、22 个缺失、没有唯一绑定 | 低 | 极高且当前证据不足 | 舍弃全量目标，只保留人工定点 |
| 把原始地图层值直接翻译成玩家楼层 | 21 个原始值，但没有地面、地下、屋顶语义 | 低且易误导 | 高 | 舍弃自动猜测，改为人工语义层 |
| 把重型本地考据图直接展示给玩家 | 已接入多个检查面板 | 负价值 | 中 | 从默认产品隐藏，仅保留开发检查入口 |

## 6. 如何处理已经投入很大的沉没成本

沉没成本本身不是继续投入的理由。正确标准是“剩余投入是否能快速产生玩家价值”。据此分为三类。

### 6.1 值得立即推进到完成

1. 在线正式图、路线算法、条件边和路线步骤展示。它们已经证明可用，剩余工作主要是产品收口。
2. 网页启动依赖拆分。现有代码已经完成大部分数据读取，改为核心必需、其余懒加载的成本很低。
3. 默认路线和阻断提示修正。这是小改动，却直接决定第一次使用是否成功。
4. 可复现的构建和审计入口。现有生成器与审计器很多，应当挑出真正生成正式图所需的一套稳定命令并锁定输出。

### 6.2 应当保留成果，但不再继续扩张

1. HKX(Havok 数据文件, Havok Data File) 几何索引、边界见证和冲突清单。
2. NVA(导航网格布置文件, Navmesh Arrangement) 原生抽象连接图。
3. 全量本地考据图及其审计报告。
4. 端点歧义、缺失覆盖和无法唯一绑定的负面证据。

这些产物已经证明哪些方向不能自动化，也可能在最终取证时被局部复用。应做一次归档清单和复现说明，然后停止日常消耗。

### 6.3 只允许严格限时推进

1. NVC(导航连接文件, Navigation Connection) 解析：两到四小时；只看能否直接产出稳定跨图连接。
2. EMEVD(事件脚本文件, Event Script) 条件归一化：只处理已精确绑定到路线端点的脚本传送。
3. MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 与在线节点绑定：只处理 Beta 验收路线和高频区域。

任何限时任务到时仍不能形成“新路线边、条件说明、入口说明或覆盖结论”，立即停止并记录结果。

## 7. Beta 的准确产品定义

Beta 不是“最终一比一”的假宣传，也不是研究数据浏览器。它应当是：

> 一个以固定在线资料为主数据源、能够按当前世界状态返回关键节点序列、通行方式、单向性、层级变化和条件说明的《艾尔登法环》多位面拓扑路线规划器。

Beta 必须提供：

- 可搜索的起点和终点；
- 赐福、入口、Boss、物品和关键地标；
- 地表、地下、遗迹或地牢的语义层；
- 有向连接；
- 门、升降梯、跳落、传送门、棺材等连接类型；
- Boss、任务、钥匙和世界状态条件；
- 路线步骤和入口说明；
- 路线证据等级；
- 无法规划时的最小相关阻断原因；
- 明确的覆盖声明和纠错入口。

Beta 明确不提供：

- 连续角色运动模拟；
- 每个导航多边形的最短步行线；
- 实时读取游戏或存档；
- 自动判断玩家已经完成的任务；
- 对未覆盖区域冒充一比一；
- 将所有在线坐标点自动连成路线。

## 8. 一个工程日内交付 Beta 的计划

### 阶段一：冻结范围和建立发布基线，预计 30 至 60 分钟

- 保存当前脏工作区快照，不覆盖先前执行流的任何成果。
- 建立独立 Beta 分支或等价发布检查点。
- 把“在线正式图可用”设为发布基线。
- 把本地考据图、几何图和研究检查器从 Beta 门槛中移除。

完成条件：任何后续任务都必须能对应一个 Beta 可见结果或验收用例。

### 阶段二：拆分启动依赖，预计 90 至 120 分钟

- 页面启动只强制读取正式图和路线配置。
- 成就、在线坐标、在线大目录、本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 和本地 EMEVD(事件脚本文件, Event Script) 索引改为按需加载。
- 本地研究文件缺失时，路线规划仍然正常启动。
- 默认页面不加载任何数千万或上亿字节的研究产物。

目标：核心初始数据维持在约 153 万字节正式图加少量配置，不再把 345 万字节本地研究索引设为首屏刚性成本。

### 阶段三：重做首个用户流程，预计 120 至 180 分钟

- 用可搜索输入替代各含 798 项的原生长下拉框。
- 搜索结果按赐福、入口、Boss、物品、区域和层级分组。
- 默认示例改为“黄金树大教堂到古兰桑克斯的雷电”，确保首次打开即可看到正确路线。
- 把“大道旁露台”保留为可选起点；若条件不足，只提示真正相关的 Boss 或建议改用已激活赐福。
- 默认隐藏在线原始坐标选择、本地地图检查器和事件检查器，放入“研究或证据”折叠区。
- 路线卡片固定显示：节点、通行方式、方向、层级变化、条件、风险、来源等级。

完成条件：不了解项目内部数据结构的玩家可在一分钟内得到第一条路线。

### 阶段四：修正阻断解释，预计 60 至 90 分钟

- 不再列出整个可达边界上所有可能条件。
- 优先返回离当前目标最近的缺失条件。
- 若更换附近赐福即可绕过缺失条件，直接给出建议起点。
- 区分“当前状态不满足”“图中尚未覆盖”“方向是单向”“目标属于另一世界状态”。

完成条件：不可达提示必须能指导下一步，而不是输出一串无关 Boss 名称。

### 阶段五：建立 E2E(端到端, End-to-End) 回归，预计 90 至 120 分钟

至少固定以下验收路线：

1. 黄金树大教堂到古兰桑克斯的雷电；
2. 史东薇尔正门到“接肢”葛瑞克；
3. 火山官邸入口到拉卡德；
4. 法姆·亚兹拉迎风露台到“黑剑”玛利喀斯；
5. 圣树树冠到玛莲妮亚；
6. 希芙拉河井底到龙人士兵；
7. 王城正常状态和灰烬状态的互斥路线；
8. 一条 DLC(可下载内容, Downloadable Content) 的升降梯、屋顶或单向路线。

每条用例检查起点、终点、路线段顺序、关键条件、单向性和层级变化。

### 阶段六：页面与发布 QA(质量保证, Quality Assurance)，预计 60 分钟

- 真实浏览器打开页面；
- 检查窄屏和常规桌面宽度；
- 检查搜索、条件切换、复制路线和错误提示；
- 检查浏览器日志；
- 检查本地服务停止后没有残留进程；
- 检查不会读取游戏进程、存档或原始安装目录；
- 生成一个明确版本号和发布说明。

## 9. Beta 发布验收标准

以下项目必须全部满足，才称为可用 Beta：

1. 页面在本地研究文件全部缺失时仍能启动正式路线规划。
2. 默认示例路线能够成功显示，而不是首屏不可达。
3. 上述 8 条 E2E(端到端, End-to-End) 路线全部通过。
4. 起点和终点支持文字搜索，不要求用户滚动数百项。
5. 条件不足时只显示与当前目标相关的缺失条件。
6. 单向跳落、传送、棺材和升降梯不会被自动反向。
7. 王城正常状态与灰烬状态不会混成同一条无条件路线。
8. 路线步骤显示入口、层级和通行方式，而不是只显示二维坐标。
9. 页面默认不加载本地考据图和 HKX(Havok 数据文件, Havok Data File) 几何数据。
10. 在线正式图审计通过，边端点、显示名、通行方式和来源无缺失。
11. 真实浏览器无项目自身错误。
12. 发布页面明确写明当前覆盖范围，禁止宣称已经完成一比一。

## 10. Beta 之后的两条独立工程线

### 10.1 产品线

产品线只接受直接改善玩家路线的工作：

- 修正错误边和错误条件；
- 增加高频目标与最近赐福路线；
- 补入口地标、楼层、屋顶和地下说明；
- 增加路线反馈和快速纠错；
- 扩展更多在线交叉核对路线；
- 提升搜索和路线阅读体验。

产品线每次提交都必须有可见结果和 E2E(端到端, End-to-End) 用例。

### 10.2 研究线

研究线负责：

- 本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 明确声明；
- 本地 EMEVD(事件脚本文件, Event Script) 传送和状态条件；
- NVC(导航连接文件, Navigation Connection) 限时格式验证；
- NVA(导航网格布置文件, Navmesh Arrangement) 作为覆盖旁证；
- 对争议路线做定点取证。

研究线永远不得阻塞产品线发布。只有能转化成“节点、边、方向、条件、层级或证据等级”的结果，才允许晋级正式图。

## 11. 工作区整理方案

工作区杂乱的直接原因是：每发现一个新格式或问题，就新增一套生成器、审计器和大型输出，并把结果继续接入同一个服务和同一份项目说明。缺少“正式产品数据、可复现构建、研究生成物、运行时工具”四类边界。

建议在 Beta 发布后立即执行一次非破坏性整理：

### 11.1 版本库保留

- 网页和本地服务；
- 在线正式图；
- Beta 真正需要的固定在线快照；
- 正式图构建脚本；
- 正式图审计脚本；
- E2E(端到端, End-to-End) 用例；
- 研究产物的摘要清单和复现说明。

### 11.2 版本库外保留

- 原始游戏副本；
- 全量解包文件；
- HKX(Havok 数据文件, Havok Data File) 几何明细；
- 数亿字节的边界见证和中间索引；
- 可重新生成的临时运行时工具；
- 本地隐私或许可证不允许提交的来源快照。

### 11.3 不立即删除任何既有成果

先生成清单、记录来源副本、构建命令、输出摘要和校验值，再迁出重型生成物。当前已有修改属于先前执行流，整理时不得用强制重置或覆盖方式处理。

## 12. 最终一比一拓扑的正确完成标准

最终一比一不是复刻游戏物理引擎。它应当满足以下可审计标准：

- 所有可玩位面都有明确地图或区域记录；
- 所有赐福、主要入口、关键岔路、垂直连接、传送、棺材、Boss 门和世界状态节点都有唯一语义身份；
- 所有关键连接都有方向和通行类型；
- 单向跳落与不可逆状态被显式标注；
- 每条条件边都能说明 Boss、任务、钥匙或世界状态要求；
- 王城等发生结构变化的区域拥有独立状态版本；
- 地表、地下、屋顶和内部楼层使用玩家可理解的语义层，而不是直接猜测原始数字层；
- 每条路线边至少有在线交叉证据、游戏文件证据或明确的人工验证等级；
- 每个区域都有固定入口到关键目标的回归路线；
- 未确认内容保留为候选，不伪装成确定路线。

XYZ(三维坐标轴, X-Y-Z Axes) 坐标可以帮助定位和绘图，但它不是拓扑完成标准；HKX(Havok 数据文件, Havok Data File) 连续几何更不是完成标准。

## 13. 立即执行顺序

1. 暂停所有 HKX(Havok 数据文件, Havok Data File)、连续步行和全量实例绑定工作。
2. 冻结现有重型研究产物，保留但不加载。
3. 从当前在线正式图切出 Beta 发布基线。
4. 拆分启动依赖。
5. 修正默认古兰桑克斯路线和阻断提示。
6. 替换超长起终点下拉框。
7. 建立 8 条固定 E2E(端到端, End-to-End) 路线。
8. 做真实浏览器 QA(质量保证, Quality Assurance)。
9. 发布明确标注覆盖边界的拓扑 Beta。
10. Beta 稳定后，再按限时规则评估 NVC(导航连接文件, Navigation Connection) 和定点 EMEVD(事件脚本文件, Event Script) 晋级。

## 14. 最终判断

当前工程不是因为缺数据而无法交付，而是因为把最终考据标准提前压到了 Beta 上，并把研究工作错误接入了玩家首屏。

最有价值的成果已经存在：在线正式图、条件化有向边、路线算法和可读路线步骤。最没有价值的继续投入方向也已经明确：连续物理步行、底层几何冲突穷举、全量导航实例一一绑定和把原始层值强行解释成玩家楼层。

因此正确策略不是推倒重来，而是立即切断研究依赖，围绕已有正式图完成产品收口。按当前代码和数据状况，一个真实可用的拓扑 Beta 应以一个专注工程日为目标；最终一比一拓扑则转为独立、可度量、不会阻塞发布的长期工程。
