# 《艾尔登法环》拓扑地图项目现状评估与 Beta 抢救计划

评估日期：2026-08-18  
评估角色：Sol  
评估对象：当前版本库、在线正式图、本地游戏数据副本生成物、路线网页和本地服务  
最高优先级：尽快交付能在局部数据缺失或错误时继续工作的拓扑 Beta，而不是继续扩张底层物理研究

修订说明：本版根据“框架与数据必须解耦、局部数据错误不得导致全局失败”的意见修订。上一版虽然要求拆分首屏依赖，但仍然把“整张正式图通过审计”写成发布门槛，容易继续形成单体数据集和全有或全无的错误模型。本版明确废除这一前提。

## 0. 术语与缩写约定

- V1(第一版, Version 1, 当前以固定在线来源建立的第一版正式图数据)。
- JSON(JavaScript 对象表示法, JavaScript Object Notation, 本项目存储和交换结构化图数据的文本格式)。
- UI(用户界面, User Interface, 玩家实际选择起终点并查看路线的页面)。
- API(应用程序编程接口, Application Programming Interface, 网页从本地服务读取图和目录数据的接口)。
- POI(兴趣点, Point of Interest, 地图上可搜索、定位或作为路线目标的具体对象)。
- DOM(文档对象模型, Document Object Model, 浏览器中全部页面元素组成的结构树)。
- MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring, 记录地图实体、区域、机关和显式跨图声明的地图文件)。
- EMEVD(事件脚本文件, Event Script, 社区沿用的格式名；记录事件指令、传送、条件和状态变化，并无可靠的官方逐字母展开)。
- NVA(导航网格布置文件, Navmesh Arrangement, 记录原生导航区域及连接声明的高层文件；英文展开是功能性称呼，不是已确认的官方逐字母释义)。
- NVC(导航连接文件, Navigation Connection, 本项目暂按功能命名的跨地图连接候选格式；官方逐字母释义尚未确认)。
- HKX(Havok 数据文件, Havok Data File, 保存导航网格或碰撞几何等底层数据；这里使用的是文件类别名，不主张它存在官方逐字母展开)。
- ObjAct(对象交互, Object Action, 门、升降梯、机关等可触发对象的交互记录)。
- E2E(端到端, End-to-End, 从打开页面、输入起终点到得到路线的完整用户流程)。
- QA(质量保证, Quality Assurance, 对数据、功能和真实用户流程进行验收)。
- DLC(可下载内容, Downloadable Content, 本项目中的《黄金树幽影》扩展区域内容)。
- XYZ(三维坐标轴, X-Y-Z Axes, 游戏空间中的三个方向坐标)。

本文不使用数学公式，因此不存在未解释的公式字母或特殊符号。

## 1. 执行结论

### 1.1 用户对产品目标的判断是正确的

这个项目真正需要的是“带条件的有向拓扑图”，不是连续步行模拟，也不是角色碰撞物理模拟。

对玩家有价值的路线只需回答：

- 从哪个赐福或入口出发；
- 依次经过哪些关键节点；
- 使用门、梯子、升降梯、传送门、棺材、跳落还是普通通道；
- 哪些连接是单向的；
- 哪些连接受 Boss、任务、钥匙或世界状态约束；
- 目标位于地表、地下、遗迹内部、屋顶还是其他语义楼层；
- 当前条件不足时，到底缺哪一个直接相关条件。

它不需要回答角色在每一个导航多边形上如何连续移动，也不需要证明每一厘米碰撞面都可行。后者的成本远高于真实产品价值，而且无法替代入口说明、楼层语义和状态条件。

### 1.2 当前并非“什么都没有”，而是“产品主线被研究主线压住了”

当前已经存在一套接近可交付的在线正式图：

- 746 个正式节点；
- 1195 条有向路线边；
- 412 个赐福节点；
- 地表、地下、遗迹或地牢三类语义层；
- 381 条带条件的边；
- 178 个不同的状态条件；
- 全图只有一个弱连通分量，没有孤立节点，也没有失效的边端点；
- 每条边均具备通行方式、路线备注和来源证据；
- 在线覆盖审计中，42 项成就里有 40 项存在目标映射，并且这 40 项在“全部已登记条件满足”的覆盖假设下都有赐福到目标的路径；其余 2 项本身没有单一空间目标。

正式图见 [data/v1/graph.json](./data/v1/graph.json)。它已经足以支撑一个诚实标注覆盖边界的拓扑 Beta。

同时，当前又生成了一个完全不同性质的本地考据图：

- 29144 个节点；
- 7976 条拓扑候选边；
- 16314 条语义关系；
- 全部 7976 条边都被强制标为不可路由；
- 全部节点和语义关系也都被强制标为不可路由。

该图见 [data/v1/entities/local-abstract-topology-graph.json](./data/v1/entities/local-abstract-topology-graph.json)。它是证据仓库，不是玩家路线图。把它继续当作 Beta 的前置条件，只会不断增加数据量，不会让玩家更早得到路线。

### 1.3 Beta 距离很近，最终一比一拓扑仍然很远

在不继续扩张本地底层研究的前提下，一个真实可用、明确标注“尚非完整一比一”、并具备数据故障隔离能力的拓扑 Beta，预计需要约 8 至 12 小时。核心是先建立数据包接口、逐记录校验和局部隔离，再复用现有路线引擎，而不是重写整个项目。

最终一比一拓扑不能诚实地给出完成百分比，因为项目目前没有建立“全量应有节点、应有边、状态版本和语义楼层”的固定分母。现有正式图覆盖大量代表性路线和成就目标，但尚不能证明覆盖了每个可玩位面、入口、屋顶、升降梯端点、单向落差和世界状态版本。完整目标至少是独立的多周工程，不能再作为 Beta 发布门槛。

### 1.4 首要架构结论：任何局部数据问题都只能局部失败

框架必须能够在没有任何数据时正常启动，只是显示“当前没有已加载地图数据”；加载任意一个有效数据包后，就应立即为这个数据包中的连通部分提供搜索和路线。数据完整度决定覆盖范围，不决定框架生死。

正确的故障边界应当是：

- 一个节点格式错误，只隔离该节点以及直接依赖它的边；
- 一条边端点不存在，只隔离该边，不删除其余节点；
- 一个条件定义缺失，只让引用该条件的边进入“条件未知”状态；
- 一个区域数据包损坏，只跳过该区域，其他区域继续工作；
- 一项在线来源暂时不可用，只影响该来源的增量更新，不影响已固定的数据；
- 两个连通分量之间尚无连接，只能得出“当前数据中无跨分量路径”，不能得出“系统失效”；
- 一次路线查询无结果，只是该次查询失败，不能让页面或图存储失效。

框架只负责装载、校验、合并、搜索、求路和报告诊断；数据只负责声明节点、边、条件、证据和覆盖范围。二者通过稳定数据契约连接，禁止让某个具体来源、某张地图或某种本地格式侵入路线引擎。

## 2. 本次评估实际验证了什么

本次没有读取正在运行的游戏、进程内存或存档。测试对象仅为版本库、本地游戏副本生成物和临时启动的回环本地服务。

### 2.1 静态和数据验证

- [app.js](./app.js) 通过 JavaScript 语法解析。
- [server.py](./server.py) 通过 Python 语法编译。
- 在线正式图的全部边均指向存在的节点。
- 在线正式图只有一个弱连通分量，且没有孤立节点。
- 节点显示名无缺失。
- 边的通行方式、说明和来源证据无缺失。
- 在线覆盖审计脚本执行成功。
- 本地考据图审计脚本执行成功，并再次确认全部候选都不可路由。

这些结果只说明“当前这份数据快照质量较好”，不应成为框架启动条件。以后即使某个新增节点或新增边没有通过校验，框架也必须装载其余有效记录并单独报告被隔离项。

### 2.2 本地服务验证

对首页、脚本、样式和首屏初始化相关数据共 21 项进行了回环请求测试：全部成功，没有失败项，总响应体约 638 万字节。

其中约 345 万字节来自本地地图和事件考据索引。这些数据对“先算出一条在线拓扑路线”不是必需的，却被当前页面初始化设为强制依赖。

### 2.3 真实页面验证

页面能够打开，路线算法能够工作，浏览器日志中没有项目自身产生的错误。

把起点改为“黄金树大教堂”后，页面成功生成以下 5 段路线：

1. 黄金树大教堂，经西侧出口或下楼梯，到大教堂西侧出口；
2. 乘电梯下降到西侧电梯下端；
3. 经下层楼梯和栏杆到古兰桑克斯上方栏杆；
4. 从栏杆单向跳落到巨矛下方平台；
5. 跳上巨龙石矛，取得古兰桑克斯的雷电。

这正是项目最初需要交付的产品形态，也证明路线引擎本身不是主要阻塞点。

### 2.4 代表路线的无界面回归结果

在相应条件满足时，以下路线均成功求解：

| 路线 | 路线段数 | 关键通行语义 |
|---|---:|---|
| 史东薇尔正门到“接肢”葛瑞克 | 5 | 遗迹内部、垂直路线、Boss 接近 |
| 火山官邸入口到拉卡德 | 5 | 隐藏路线、垂直路线、Boss 完成状态、传送 |
| 法姆·亚兹拉迎风露台到“黑剑”玛利喀斯 | 8 | 浮空岩、内部路线、Boss 状态、屋顶路线 |
| 圣树树冠到玛莲妮亚 | 9 | 垂直路线、屋顶、升降梯、腐败树根路线 |
| 希芙拉河井底到龙人士兵 | 5 | 地下通道、升降梯、倒塌石柱、传送门 |

这些结果进一步说明：当前缺的是可用产品收口，不是继续解析数千万条几何边。

### 2.5 已发现的直接产品缺陷

最重要的缺陷不是底层数据缺失，而是首个用户流程就配置错误：

- 页面默认目标是“古兰桑克斯的雷电”；
- 页面默认起点却是“大道旁露台”；
- 默认条件不包含击败王城金色幻影 Boss 的状态；
- 因而首屏直接显示“此刻不可达”；
- 阻断提示列出多个与这条局部路线无关的 Boss 条件，而不是指出“应从黄金树大教堂出发”或“尚未击败必要 Boss”。

也就是说，最能证明项目价值的示例路线实际上已经存在，但默认入口把它隐藏成了失败案例。

## 3. 为什么做了约 24 小时仍没有“大体能用”的 Beta

### 3.1 把“最终一比一”错误地解释成了“物理连续可行性”

“最终一比一”本来应当约束拓扑覆盖：节点是否齐全、边是否有向、条件是否正确、楼层是否清楚、世界状态是否区分。

执行中却逐渐把它解释成：必须解码底层导航几何、核验连接面的索引、证明连续步行、绑定每一个原生导航实例。这个解释把产品从“知识图谱和路线规划器”变成了“半个游戏导航运行时的离线重建”。二者预算并不在同一数量级。

### 3.2 证据晋级门槛被设成了“非完美即不可用”

本地考据链中的每条边默认不可路由，只有在端点、方向、状态、楼层和玩家空间全部精确绑定后才考虑晋级。这个策略适合最终取证层，却不适合在线优先的 Beta。

更合理的逐记录分级应当是：

- 已由多个在线攻略交叉核对，可用于路线；
- 已由游戏文件明确声明，可用于路线；
- 结构有效但非关键字段缺失，可降级用于路线并显示警告；
- 有高可信候选，但尚不参与默认路线；
- 结构或引用无效，进入隔离区；
- 仅为证据关系，不参与路线，但仍可被检查。

当前执行把大量记录压缩成了最后一类，因此解析越多，玩家可走的边仍然是零。反过来，也不能因为最后两类中出现错误就拒绝发布前几类已经正确的数据。

### 3.3 研究数据被接进了首屏关键路径

当前 [app.js](./app.js) 在初始化时并行请求正式图、成就目录、路线目录、在线地图索引、本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 索引和本地 EMEVD(事件脚本文件, Event Script) 索引等 18 项数据，并把多数请求失败视为整个页面启动失败。

用于查路线的核心实际上只需要正式图和路线配置。坐标证据、在线大目录、本地考据索引和研究面板都应按需加载，任何一个缺失都不应阻止路线规划器启动。

这不仅是“首屏太重”，而是明确的框架和数据强耦合：框架知道每一种具体数据源，请求列表写死在启动函数中，任何一个可选来源失败都会进入同一个全局异常分支。正式图本身又以一个整体文件装载和审计，没有独立区域数据包、逐记录隔离区和部分成功状态。

需要准确区分：当前绘边代码已经会跳过端点不存在的单条边，因此并非实测证明“一条坏边必然立刻让路线循环崩溃”。真正的问题是这种局部保护没有上升为框架契约，也没有覆盖启动、数据包解析、合并、搜索和发布流程。现在的局部容错属于偶然实现细节，不能作为系统保证。

必须同时解除两层耦合：

- 启动耦合：框架启动不等待任何业务数据源全部成功；
- 数据集耦合：一份数据包、一条记录或一个连通分量失败，不影响其他有效数据包和连通分量。

### 3.4 页面把研究控制台和玩家产品混在了一起

实测首屏生成：

- 11777 个 DOM(文档对象模型, Document Object Model) 元素；
- 4002 个下拉选项；
- 起点和终点各 798 个选项；
- 在线地图选择 1037 个选项；
- 本地地图选择 1347 个选项；
- 图层中同时渲染约 798 个节点元素和 1576 个边元素。

这不是“数据不够”，而是“没有给普通玩家一个小而明确的入口”。玩家需要的是搜索起点、搜索目标、勾选少量状态、得到路线；不是在 4002 个下拉选项和本地文件检查器之间判断该点哪个数据源。

### 3.5 缺少以玩家任务为核心的验收顺序

如果第一条固定验收就是“黄金树大教堂到古兰桑克斯的雷电”，默认配置错误会在最早阶段暴露。当前大量审计验证的是解析字段完整、哈希一致和候选未被错误晋级，却没有把“用户打开页面后是否在一分钟内得到一条有用路线”设为第一发布门槛。

同样缺少“故意注入一条坏边后，其余路线仍然可用”的框架验收。没有这类测试，就很容易把数据审计失败误写成系统不可运行。

### 3.6 版本库没有形成可发布检查点

当前工作区有：

- 7 个已跟踪文件被修改；
- 74 个未跟踪文件；
- 249 个已跟踪文件；
- `data` 目录约 7.42 亿字节；
- `data/v1/entities` 下 34 个实体 JSON(JavaScript 对象表示法, JavaScript Object Notation) 文件合计约 7.37 亿字节；
- `scripts` 目录有 53 个当前脚本源文件；
- 被忽略的运行时工具目录约 970 万字节。

版本库原始游戏文件隔离做得正确，但“产品代码、轻量正式数据、重型研究生成物、研究工具和运行时产物”没有彻底分道。结果是无法明确指出哪一个提交就是 Beta，也很难做干净部署和回滚。

### 3.7 对先前执行流的客观评价

从生成物看，先前执行流并非没有工作，而是在错误的完成定义上高强度产出：大量底层数据已被成功解析、交叉绑定和审计。问题不在于产出数量，而在于这些产出几乎都被设计为不可路由，对用户首个任务没有形成增量价值。

因此根因是产品排序、验收标准和停止规则失控，而不是单个解析器是否足够复杂。继续沿原方向堆数据只会放大偏差。

## 4. 各类本地游戏数据究竟有什么价值

### 4.1 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring)

高价值信息包括：

- 地图编号和地图之间的显式目标声明；
- 门、升降梯、机关、碰撞部件、区域和入口实体；
- 部件的游戏空间位置；
- 某些连接对象的两端或目标地图；
- 可用于把在线攻略中的地点名称绑定到游戏原生实体。

它非常适合证明“这个入口属于哪张地图”“这个机关或连接对象实际存在”。它不必先绑定到每一个导航网格实例，才能支持抽象拓扑。

结论：保留并继续做有明确产品用途的定点绑定，但不再要求全量实例级精确绑定后才发布 Beta。

### 4.2 EMEVD(事件脚本文件, Event Script)

高价值信息包括：

- 精确传送指令；
- 事件触发前提；
- Boss 击败、任务旗标和世界状态变化；
- 门、升降梯和 ObjAct(对象交互, Object Action) 的启用或禁用；
- 王城前后状态、剧情传送和特殊进入方式。

它最适合补“条件”和“非步行传送”。但完整解释全部事件脚本需要运行时状态、保存数据和玩家上下文，静态分析无法可靠求出所有条件的当前真值。

结论：只提取与明确拓扑边直接相关的传送和条件，不做完整事件虚拟机，不以“静态求出所有运行时真值”为 Beta 门槛。

### 4.3 NVA(导航网格布置文件, Navmesh Arrangement)

当前已经解析：

- 997 个文件；
- 9480 个原生导航区域节点；
- 5884 个连接声明；
- 7201 个无向原生连通候选。

这些信息能证明同一地图内部某些原生导航区域存在连接关系，对最终拓扑覆盖审计有辅助价值。但它并不天然告诉我们：玩家该从哪个门进入、连接是上楼还是下楼、是否单向、是否被 Boss 门锁住、该区域应显示为地下几层。

结论：现有抽象连接图已经足够作为研究证据，立即冻结扩张；不把它作为 Beta 依赖。

### 4.4 NVC(导航连接文件, Navigation Connection)

当前已经从游戏副本中提取 3674 个候选文件，文件名直接带两个地图编号，因此它可能比底层几何更直接地提供跨地图连接候选。

但二进制字段语义尚未完成验证。它的价值取决于能否在很短时间内稳定输出“源地图、目标地图、连接类型或端点引用”。

结论：放到 Beta 之后做一次严格限时的可行性冲刺。两到四小时内若不能稳定产生可审计的跨图连接记录，就归档停止，不再无期限逆向。

### 4.5 HKX(Havok 数据文件, Havok Data File)

当前已解码的底层数据包括：

- 3390 个导航网格条目；
- 16607263 个顶点；
- 6888218 个面；
- 29901878 条几何边；
- 137358 组连接边界见证；
- 其中还有 9824 组索引空间冲突需要进一步解释。

这批数据对“连续物理可行性、精确边界和导航多边形”有价值，对“从哪个赐福经过哪个升降梯到哪个 Boss”价值极低。即使把全部冲突解决，也不会自动得到门的名称、路线方向、任务条件或玩家可理解的楼层。

结论：立即停止继续解码、扩张和冲突消解。保留现有产物和复现记录，不删除，但它们不得进入产品关键路径。

## 5. 技术方向取舍

| 方向 | 当前状态 | 用户价值 | 剩余成本 | 决策 |
|---|---|---|---|---|
| 在线正式拓扑图和路线引擎 | 已能跑代表路线 | 极高 | 低 | 立即推进到 Beta |
| 起终点搜索、条件输入和路线说明 | 已有功能但入口臃肿 | 极高 | 低到中 | 立即重构为玩家主流程 |
| 在线 POI(兴趣点, Point of Interest) 搜索和固定快照 | 数据丰富 | 高 | 低 | 保留，按需加载，不阻塞路线 |
| MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 显式地图声明 | 已大量提取 | 中到高 | 中 | 只做定点晋级和最终拓扑校验 |
| EMEVD(事件脚本文件, Event Script) 精确传送和条件 | 已有 15 条精确脚本传送及更多候选 | 中到高 | 中 | Beta 后定点推进，不做全解释器 |
| NVC(导航连接文件, Navigation Connection) 跨图候选 | 文件已提取，格式未完成 | 潜在中到高 | 不确定 | Beta 后限时两到四小时验证 |
| NVA(导航网格布置文件, Navmesh Arrangement) 抽象连接 | 核心提取已完成 | 中 | 继续扩张成本高 | 冻结，作为研究证据 |
| HKX(Havok 数据文件, Havok Data File) 几何和连续步行 | 已产生巨量结果 | 对本产品低 | 极高 | 立即停止，保留归档 |
| 全量导航实例与地图端点一一绑定 | 1125 个端点中 1103 个歧义、22 个缺失、没有唯一绑定 | 低 | 极高且当前证据不足 | 舍弃全量目标，只保留人工定点 |
| 把原始地图层值直接翻译成玩家楼层 | 21 个原始值，但没有地面、地下、屋顶语义 | 低且易误导 | 高 | 舍弃自动猜测，改为人工语义层 |
| 把重型本地考据图直接展示给玩家 | 已接入多个检查面板 | 负价值 | 中 | 从默认产品隐藏，仅保留开发检查入口 |

## 6. 如何处理已经投入很大的沉没成本

沉没成本本身不是继续投入的理由。正确标准是“剩余投入是否能快速产生玩家价值”。据此分为三类。

### 6.1 值得立即推进到完成

1. 在线正式图、路线算法、条件边和路线步骤展示。它们已经证明可用，剩余工作主要是产品收口。
2. 网页启动依赖拆分。现有代码已经完成大部分数据读取，改为核心必需、其余懒加载的成本很低。
3. 默认路线和阻断提示修正。这是小改动，却直接决定第一次使用是否成功。
4. 可复现的构建和审计入口。现有生成器与审计器很多，应当挑出真正生成正式图所需的一套稳定命令并锁定输出。

### 6.2 应当保留成果，但不再继续扩张

1. HKX(Havok 数据文件, Havok Data File) 几何索引、边界见证和冲突清单。
2. NVA(导航网格布置文件, Navmesh Arrangement) 原生抽象连接图。
3. 全量本地考据图及其审计报告。
4. 端点歧义、缺失覆盖和无法唯一绑定的负面证据。

这些产物已经证明哪些方向不能自动化，也可能在最终取证时被局部复用。应做一次归档清单和复现说明，然后停止日常消耗。

### 6.3 只允许严格限时推进

1. NVC(导航连接文件, Navigation Connection) 解析：两到四小时；只看能否直接产出稳定跨图连接。
2. EMEVD(事件脚本文件, Event Script) 条件归一化：只处理已精确绑定到路线端点的脚本传送。
3. MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 与在线节点绑定：只处理 Beta 验收路线和高频区域。

任何限时任务到时仍不能形成“新路线边、条件说明、入口说明或覆盖结论”，立即停止并记录结果。

## 7. Beta 的准确产品定义

Beta 不是“最终一比一”的假宣传，也不是研究数据浏览器。它应当是：

> 一个以固定在线资料为主数据源、能够按当前世界状态返回关键节点序列、通行方式、单向性、层级变化和条件说明的《艾尔登法环》多位面拓扑路线规划器。

Beta 必须提供：

- 可搜索的起点和终点；
- 任意有效数据包加载完成后立即可用，不等待全量数据；
- 每个已加载连通分量独立搜索和求路；
- 被隔离记录、缺失数据包和当前覆盖范围的清晰诊断；
- 赐福、入口、Boss、物品和关键地标；
- 地表、地下、遗迹或地牢的语义层；
- 有向连接；
- 门、升降梯、跳落、传送门、棺材等连接类型；
- Boss、任务、钥匙和世界状态条件；
- 路线步骤和入口说明；
- 路线证据等级；
- 无法规划时的最小相关阻断原因；
- 明确的覆盖声明和纠错入口。

Beta 明确不提供：

- 连续角色运动模拟；
- 每个导航多边形的最短步行线；
- 实时读取游戏或存档；
- 自动判断玩家已经完成的任务；
- 对未覆盖区域冒充一比一；
- 将所有在线坐标点自动连成路线。

Beta 的“可用”按已加载、已验证的数据范围判断，不按全世界是否完整判断。部分覆盖是正常产品状态，不是异常状态。

## 8. 框架与数据完全解耦的目标架构

### 8.1 框架层只实现通用能力

框架层只包含以下职责：

- 数据包发现和版本选择；
- 节点、边和条件的逐记录结构校验；
- 有效记录合并；
- 无效记录隔离与诊断；
- 连通分量计算；
- 起终点搜索；
- 条件化有向求路；
- 覆盖范围和降级状态展示。

框架层不得写死 MapForGoblins、本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring)、本地 EMEVD(事件脚本文件, Event Script) 或任何具体区域。每一种来源都只能通过独立适配器生成同一数据契约，路线引擎永远不认识来源格式。

框架本身必须在零数据状态下正常打开。零数据只意味着没有可规划路线，不意味着程序错误。

### 8.2 数据以独立、可替换的数据包发布

每个数据包至少声明：

- 数据包名称和版本；
- 覆盖的区域、位面或世界状态；
- 节点集合；
- 边集合；
- 条件定义；
- 跨包依赖；
- 来源和生成时间；
- 已知缺口和被隔离记录数量。

数据包的物理格式也必须保留记录边界，不能再把数千条记录塞进一个“任意位置语法损坏就完全无法解析”的单体数组。清单与记录分离，记录采用可独立解析的行或小分块；这样单条记录损坏时仍能读取同包中的其他记录。只有数据包清单本身损坏时才跳过该包，而且仍不影响其他包。

第一版拆分不需要手工重做 746 个节点。可以从现有正式图机械生成若干独立包，例如：

- 主世界地表包；
- 地下世界包；
- 各主要遗迹或地牢包；
- 《黄金树幽影》区域包；
- 王城正常状态包；
- 灰烬王城状态包；
- 跨区域传送、升降梯和入口桥接包。

区域包保存区域内部拓扑，桥接包只保存跨包边。桥接包损坏时，各区域内部仍然完全可用；某个区域包缺失时，其他区域照常搜索和求路。

### 8.3 校验必须逐记录执行，并设置隔离区

装载器不能因为发现一个坏记录就拒绝整包，更不能拒绝整个系统。处理规则如下：

| 数据问题 | 正确处理 | 允许继续工作的范围 |
|---|---|---|
| 节点缺少非关键显示字段 | 使用后备显示名并标记降级 | 节点和相关路线继续可用 |
| 节点结构无法解析 | 隔离该节点 | 其他节点继续可用 |
| 边引用不存在的端点 | 隔离该边 | 两端以外的全部图继续可用 |
| 边缺少风险或相对成本 | 使用明确默认值并标记降级 | 该边可按策略继续使用 |
| 边引用未知条件 | 标为条件未知，默认不自动放行 | 其他边继续可用 |
| 节点标识符冲突 | 保留已发布稳定记录，隔离冲突增量 | 已发布路线不回退 |
| 单个数据包语法损坏 | 跳过该包并报告 | 其他数据包继续可用 |
| 跨包桥接数据缺失 | 保留各自连通分量 | 每个分量内部继续可用 |
| 来源证据暂缺 | 降低证据等级或保留候选状态 | 不影响其他已确认记录 |

隔离区必须保留原记录、错误原因、来源和首次发现版本，便于后续“缺啥补啥”。修正后只重新发布对应记录或数据包，不重建整套系统。

### 8.4 连通分量是正常服务单元，不是失败残片

图不必全局连成一块才能有价值。框架加载数据后应计算当前有效图的连通分量，并让每个分量独立提供：

- 节点搜索；
- 分量内部路线；
- 分量入口和出口；
- 与其他分量之间缺少的桥接类型；
- 当前覆盖范围。

当起点和终点位于不同分量时，结果应区分：

- 已知存在连接，但对应条件未满足；
- 桥接数据包尚未加载；
- 当前资料尚未收录连接；
- 已知二者本来不可达；
- 相关候选边正在隔离区等待修正。

这些都是数据状态，不是框架错误。即使世界被拆成数百个独立小分量，所有已确认的小分量也应立即可用。

### 8.5 路线查询只依赖本次相关子图

路线引擎应从规范化后的有效记录中提取本次查询相关的子图，而不是要求全量数据先达到同一质量等级。

一条路线所经过的节点和边都有效，即可返回路线。图中其他区域存在缺口、错误节点或未验证边，与本次结果无关。路线结果同时携带所用数据包版本和证据等级，确保局部结果可追溯。

### 8.6 发布和更新按数据包独立进行

数据更新流程应为：

1. 新记录进入暂存数据包；
2. 对该包逐记录校验；
3. 有效记录形成不可变版本；
4. 无效记录进入隔离区；
5. 清单切换到新版本；
6. 框架按需装载新版本；
7. 出现回归时只回退该数据包。

任何数据包都不要求其他包同时更新。发布王城修复不需要重新发布希芙拉河；修复一条升降梯边不需要让全部 1195 条边重新通过全局门禁。

### 8.7 当前项目的最短迁移路径

不需要推翻现有路线引擎。最短迁移方式是：

1. 在现有图和路线引擎之间加入统一装载器与规范化层；
2. 把当前正式图机械拆成首批区域包和桥接包；
3. 装载时逐节点、逐边校验，输出有效图和隔离诊断；
4. 路线引擎只接收有效图，不直接读取任何来源文件；
5. 搜索界面从已加载数据包聚合节点；
6. 本地考据数据和在线增量都通过独立适配器生成候选包；
7. 候选包是否存在、是否损坏，都不影响已发布包。

### 8.8 真正允许导致框架失败的情况

只有框架代码自身无法载入、核心数据契约版本完全不受支持，或运行环境无法提供基本文件读取时，才属于框架失败。

以下情况一律不得导致框架失败：没有数据、部分数据缺失、单个文件损坏、节点冲突、边悬空、条件未知、证据不足、来源离线、区域未完成、跨分量无路径。

## 9. 8 至 12 小时内交付解耦 Beta 的计划

### 阶段一：冻结范围和建立发布基线，预计 30 至 45 分钟

- 保存当前脏工作区快照，不覆盖先前执行流的任何成果。
- 建立独立 Beta 分支或等价发布检查点。
- 把本地考据图、几何图和研究检查器从 Beta 门槛中移除。
- 明确“部分数据可用就是正常运行状态”。

完成条件：任何后续任务都必须改善框架容错、已知区域路线或玩家主流程。

### 阶段二：建立数据包装载器和逐记录隔离，预计 90 至 150 分钟

- 定义与来源无关的节点、边、条件和数据包契约。
- 装载器逐节点、逐边校验，而不是整包一次判死。
- 有效记录进入活动图，无效记录进入隔离区。
- 重复标识符、悬空边、未知条件和非关键字段缺失分别处理。
- 路线引擎只读取规范化后的活动图。

完成条件：向测试数据加入一个坏节点和一条坏边后，其余既有路线结果完全不变。

### 阶段三：把现有正式图机械拆成首批独立包，预计 60 至 90 分钟

- 按主世界地表、地下、主要遗迹或地牢、《黄金树幽影》区域和世界状态拆包。
- 把跨区域边放入独立桥接包。
- 生成轻量数据包清单，记录版本、覆盖和隔离数量。
- 不手工改写现有 746 个节点和 1195 条边，避免无价值返工。

完成条件：删除或破坏任意一个非核心区域包后，其他区域内部路线仍可搜索和求解。

### 阶段四：拆除启动函数中的数据源硬编码，预计 60 至 90 分钟

- 页面先启动框架，再读取数据包清单。
- 任意有效数据包到达后立即更新搜索和路线能力。
- 成就、在线坐标、在线大目录、本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 和本地 EMEVD(事件脚本文件, Event Script) 索引全部按需加载。
- 单个请求失败只显示对应数据包状态，不进入全局失败页面。
- 默认页面不加载任何数千万或上亿字节的研究产物。

完成条件：框架在零数据、单包、部分包和全部包四种状态下都能正常运行。

### 阶段五：重做首个用户流程，预计 120 至 180 分钟

- 用可搜索输入替代各含 798 项的原生长下拉框。
- 搜索结果按已加载数据包、赐福、入口、Boss、物品、区域和层级分组。
- 默认示例改为“黄金树大教堂到古兰桑克斯的雷电”。
- 把“大道旁露台”保留为可选起点；若条件不足，只提示真正相关的 Boss 或建议改用已激活赐福。
- 默认隐藏在线原始坐标选择、本地地图检查器和事件检查器。
- 路线卡片显示所用数据包版本、节点、通行方式、方向、层级变化、条件、风险和来源等级。

完成条件：不了解项目内部数据结构的玩家可在一分钟内得到第一条路线。

### 阶段六：修正阻断解释并增加故障注入测试，预计 90 至 120 分钟

- 不再列出整个可达边界上所有可能条件。
- 区分条件未满足、桥接包缺失、当前未收录、已知不可达和记录被隔离。
- 故意测试坏节点、坏边、未知条件、损坏区域包和缺失桥接包。
- 验证每次故障只影响对应记录、数据包或跨包路线。

完成条件：任何局部故障都不能清空全部搜索结果、全部图或全部路线能力。

### 阶段七：路线回归和发布 QA(质量保证, Quality Assurance)，预计 90 至 120 分钟

至少固定以下 E2E(端到端, End-to-End) 路线：

1. 黄金树大教堂到古兰桑克斯的雷电；
2. 史东薇尔正门到“接肢”葛瑞克；
3. 火山官邸入口到拉卡德；
4. 法姆·亚兹拉迎风露台到“黑剑”玛利喀斯；
5. 圣树树冠到玛莲妮亚；
6. 希芙拉河井底到龙人士兵；
7. 王城正常状态和灰烬状态的互斥路线；
8. 一条 DLC(可下载内容, Downloadable Content) 的升降梯、屋顶或单向路线。

每条用例检查路线段顺序、关键条件、单向性、层级变化和使用的数据包。随后用真实浏览器检查搜索、条件切换、复制路线、部分数据提示、浏览器日志和发布说明。

## 10. Beta 发布验收标准

以下项目是框架行为和玩家主流程的发布标准，必须全部满足；它们不要求全部业务数据同时正确或完整：

1. 框架在没有任何业务数据时仍能打开，并明确显示零数据状态。
2. 只加载任意一个有效区域包时，该区域的搜索和内部路线立即可用。
3. 注入一个无法解析的节点时，只隔离该节点和直接依赖边。
4. 注入一条悬空边时，只隔离该边。
5. 损坏一个区域包时，其他区域包和既有路线继续工作。
6. 移除桥接包时，各连通分量内部路线继续工作，跨分量查询准确提示桥接数据缺失。
7. 未知条件只影响引用它的边，不清空其他路线。
8. 活动图中不允许保留未报告的坏引用；坏记录必须进入可检查的隔离区，而不是让整个数据发布失败。
9. 页面在本地研究文件全部缺失时仍能启动正式路线规划。
10. 默认示例路线能够成功显示，而不是首屏不可达。
11. 上述 8 条 E2E(端到端, End-to-End) 路线在对应数据包已加载时全部通过。
12. 起点和终点支持文字搜索，不要求用户滚动数百项。
13. 条件不足时只显示与当前目标相关的缺失条件。
14. 单向跳落、传送、棺材和升降梯不会被自动反向。
15. 王城正常状态与灰烬状态不会混成同一条无条件路线。
16. 路线步骤显示入口、层级、通行方式、数据包版本和证据等级。
17. 页面默认不加载本地考据图和 HKX(Havok 数据文件, Havok Data File) 几何数据。
18. 一个新数据包含有隔离记录时，可以发布其中的有效记录；隔离数量和原因必须可见。
19. 真实浏览器无项目自身错误，局部数据错误不得进入全局异常页面。
20. 发布页面明确写明已加载覆盖范围、缺失数据包和隔离记录，禁止宣称已经完成一比一。

## 11. Beta 之后的三条独立工程线

### 11.1 框架线

框架线只维护通用能力：

- 数据包契约和版本兼容；
- 逐记录校验与隔离；
- 数据包装载、卸载和回退；
- 连通分量管理；
- 搜索、求路和诊断；
- 局部故障注入测试。

框架测试使用人工构造的小图和坏数据，不依赖《艾尔登法环》全量数据，更不依赖任何单一在线来源或本地文件格式。

### 11.2 产品数据线

产品数据线只接受直接改善玩家路线的数据工作：

- 修正错误边和错误条件；
- 增加高频目标与最近赐福路线；
- 补入口地标、楼层、屋顶和地下说明；
- 增加路线反馈和快速纠错；
- 扩展更多在线交叉核对路线；
- 发布独立区域包和桥接包。

产品数据线每次提交都必须有对应数据包审计和 E2E(端到端, End-to-End) 用例。一个区域的数据发布不等待其他区域完成。

### 11.3 研究线

研究线负责：

- 本地 MSBE(《艾尔登法环》地图工作室二进制, Map Studio Binary for Elden Ring) 明确声明；
- 本地 EMEVD(事件脚本文件, Event Script) 传送和状态条件；
- NVC(导航连接文件, Navigation Connection) 限时格式验证；
- NVA(导航网格布置文件, Navmesh Arrangement) 作为覆盖旁证；
- 对争议路线做定点取证。

研究线永远不得阻塞框架线或产品数据线发布。只有能转化成“节点、边、方向、条件、层级或证据等级”的结果，才允许生成新的候选数据包；候选包仍要通过逐记录校验，不能直接写入活动图。

## 12. 工作区整理方案

工作区杂乱的直接原因是：每发现一个新格式或问题，就新增一套生成器、审计器和大型输出，并把结果继续接入同一个服务和同一份项目说明。缺少“框架、已发布数据包、来源适配器、研究生成物、运行时工具”五类边界。

建议在 Beta 发布后立即执行一次非破坏性整理：

### 12.1 版本库保留

- 与业务数据无关的框架、网页和本地服务；
- 框架的小型人工测试图和故障注入用例；
- 按区域、位面和世界状态拆分的已发布数据包；
- 单独的跨包桥接数据；
- 数据包清单、覆盖报告和隔离诊断；
- Beta 真正需要的固定在线快照；
- 各来源到统一数据契约的独立适配器；
- 数据包构建和逐记录审计脚本；
- E2E(端到端, End-to-End) 用例；
- 研究产物的摘要清单和复现说明。

### 12.2 版本库外保留

- 原始游戏副本；
- 全量解包文件；
- HKX(Havok 数据文件, Havok Data File) 几何明细；
- 数亿字节的边界见证和中间索引；
- 可重新生成的临时运行时工具；
- 本地隐私或许可证不允许提交的来源快照。

### 12.3 不立即删除任何既有成果

先生成清单、记录来源副本、构建命令、输出摘要和校验值，再迁出重型生成物。当前已有修改属于先前执行流，整理时不得用强制重置或覆盖方式处理。

## 13. 最终一比一拓扑的正确完成标准

最终一比一不是复刻游戏物理引擎。它应当满足以下可审计标准：

- 完成度按区域数据包、世界状态和桥接边分别记录，不使用一个全局真假值；
- 已完成区域可以独立达到正式质量，后续缺口不使其失效；
- 所有可玩位面都有明确地图或区域记录；
- 所有赐福、主要入口、关键岔路、垂直连接、传送、棺材、Boss 门和世界状态节点都有唯一语义身份；
- 所有关键连接都有方向和通行类型；
- 单向跳落与不可逆状态被显式标注；
- 每条条件边都能说明 Boss、任务、钥匙或世界状态要求；
- 王城等发生结构变化的区域拥有独立状态版本；
- 地表、地下、屋顶和内部楼层使用玩家可理解的语义层，而不是直接猜测原始数字层；
- 每条路线边至少有在线交叉证据、游戏文件证据或明确的人工验证等级；
- 每个区域都有固定入口到关键目标的回归路线；
- 未确认内容保留为候选，不伪装成确定路线。

最终一比一是所有独立正式数据包和桥接包逐步达到完整覆盖后的结果，不是某一天把整张世界图从“失败”一次性翻转成“成功”。任何时刻，已经正确的部分都应保持可用。

XYZ(三维坐标轴, X-Y-Z Axes) 坐标可以帮助定位和绘图，但它不是拓扑完成标准；HKX(Havok 数据文件, Havok Data File) 连续几何更不是完成标准。

## 14. 立即执行顺序

1. 暂停所有 HKX(Havok 数据文件, Havok Data File)、连续步行和全量实例绑定工作。
2. 冻结现有重型研究产物，保留但不加载。
3. 把框架从所有具体数据源和具体文件路径中解耦。
4. 加入统一数据包装载器、逐记录校验和隔离区。
5. 把当前在线正式图机械拆成区域包、状态包和桥接包。
6. 拆分启动依赖，让任意有效数据包独立提供服务。
7. 加入坏节点、坏边、损坏数据包和缺失桥接包的故障注入测试。
8. 修正默认古兰桑克斯路线、阻断提示和超长起终点下拉框。
9. 建立 8 条固定 E2E(端到端, End-to-End) 路线。
10. 做真实浏览器 QA(质量保证, Quality Assurance)。
11. 发布明确标注已加载覆盖、缺失包和隔离记录的拓扑 Beta。
12. Beta 稳定后，再按限时规则评估 NVC(导航连接文件, Navigation Connection) 和定点 EMEVD(事件脚本文件, Event Script) 晋级。

## 15. 最终判断

当前工程不是因为缺数据而无法交付，而是因为把最终考据标准提前压到了 Beta 上，把研究工作错误接入了玩家首屏，并把“某份数据是否完整”与“框架是否能够工作”混成了同一个状态。

最有价值的成果已经存在：在线正式图、条件化有向边、路线算法和可读路线步骤。最没有价值的继续投入方向也已经明确：连续物理步行、底层几何冲突穷举、全量导航实例一一绑定和把原始层值强行解释成玩家楼层。

因此正确策略不是推倒重来，而是把现有正式图降回“第一个已发布数据集合”的正确身份，在它和路线引擎之间建立稳定数据包契约、逐记录隔离和独立连通分量服务。框架不拥有任何具体游戏数据，数据错误也没有权力让框架停机。

按当前代码和数据状况，一个具备这种故障隔离能力的真实拓扑 Beta 以约 8 至 12 小时为合理目标。后续任何区域都按“正确部分立即发布、错误记录单独隔离、缺失部分持续补齐”的方式增长；最终一比一拓扑只是这些独立成果逐步汇合后的长期结果，不再存在“一条边错误导致全额失败”的发布模型。
