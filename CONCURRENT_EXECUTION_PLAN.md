# 《艾尔登法环》地图项目与前台游戏并行执行方案

> 文档版本：1.1
> 编制日期：2026-08-17（Asia/Shanghai）
> 适用主机：当前 Windows 主机
> 核心目标：项目可以在用户游玩《艾尔登法环》时继续推进，同时不注入游戏、不抢前台、不弹窗，并把共享硬件资源造成的干扰控制到可验证的低水平。

## 0. 结论

项目和游戏在进程层面可以完全隔离，但二者仍共享 CPU、内存、磁盘、GPU、网络和杀毒扫描资源。因此“互不干扰”必须通过分级通道、物理磁盘隔离、资源限额和自动停让共同实现，不能只靠“不读取游戏内存”。

并行模式分为四条执行通道：

| 通道 | 游戏运行时 | 主要用途 |
|---|---|---|
| `GAMING_SAFE` | 始终允许 | 文档、数据模型、Schema、来源/许可证审计、小型单元测试和 JSON 校验 |
| `ONLINE_INGEST` | 满足限流与许可门后允许 | 获取小型文本/JSON Source Snapshot，规范化真实在线 POI、条目和路线证据 |
| `SNAPSHOT_IO` | 满足独立物理盘和资源限额后允许 | 从预先制作的只读游戏快照限速解析地图归档，不接触正在运行的游戏安装 |
| `IDLE_ONLY` | 禁止 | 直接读取游戏 BDT、制作快照、大规模解包、工具安装/编译、地图渲染、Smithbox GUI 和实机路线采集 |

当前主机尚未准备独立快照盘，因此游戏运行期间启用 `GAMING_SAFE + ONLINE_INGEST`。Phase 1A 可直接建立真实在线 V1；Phase 1B 的本地归档提取进入队列，不阻塞在线数据、Schema、路线模型和验证器继续推进。

## 1. 不干扰的硬边界

游戏运行期间必须始终满足：

1. 不向 `eldenring.exe`、EAC、Steam 游戏进程或窗口发送任何消息、按键、鼠标、DLL、调试器、句柄操作或焦点切换；
2. 不读取或写入游戏进程内存；
3. 不修改游戏目录、`regulation.bin`、存档、配置、Steam manifest 或 EAC 文件；
4. 不启动 Smithbox、游戏、启动器、安装器、浏览器 GUI、UAC 提示、系统通知、Toast、声音或任何可抢前台的窗口；
5. 不提供传送、修改器、Overlay 或实时注入功能；
6. 所有后台任务必须无交互、隐藏窗口、失败只写日志，不弹错误框；
7. 不自动添加杀毒排除项、改电源计划、改进程优先级或系统服务；资源限制只作用于本项目自己的工作进程；
8. 用户创建 `PAUSE_GAMING_WORK.flag` 后，调度器不得启动新任务，并在当前原子块结束后暂停。

## 2. 共享资源风险

即使不接触游戏进程，下列行为仍会影响帧时间或加载：

- 从游戏所在物理盘连续读取几十 GB 的 BDT；
- 在同一盘写入大量解包小文件，引发文件系统和 Defender 扫描；
- 编译大型 .NET/Rust/Node 工具；
- CPU 密集解压、纹理解码、地图拼接和图构建；
- WebGL/GPU 地图渲染；
- 大量下载占用在线游戏网络；
- 内存占用过高导致页面文件活动。

所以“后台进程”不自动等于“无干扰进程”。这些操作必须进入 `SNAPSHOT_IO` 或 `IDLE_ONLY`。

## 3. `GAMING_SAFE` 通道

游戏运行时允许的工作：

- 修改执行文档、Schema、关系类型、RouteRequest/StateSnapshot/RouteResult 契约；
- 编写和审阅来源/许可证台账；
- 对已经位于项目目录的小型 JSON/Markdown/测试夹具做校验；
- 编写解析器接口、测试向量和静态类型，但不运行大型构建；
- 审阅第三方源码和公开文档；
- 进行低流量文本检索，不下载 SDK、归档、模型或大文件；
- 设计 UI、路线步骤和验收用例，不启动 GPU 渲染预览；
- 提交 Git 文档/Schema 变更。

限制：

- 单任务读取或写入项目文件不超过 16 MiB；
- 不遍历游戏目录，不读取 BDT，不做递归哈希；
- 不执行依赖安装、全量构建、浏览器 E2E、截图或视频处理；
- 不访问 D:；
- 同时只能有一个工作任务。

### 3.1 `ONLINE_INGEST` 子通道

在线接入必须同时满足：

- 来源已登记在 `data/online-source-registry.json`，且允许接入的字段、许可证、revision/commit 和禁用资产明确；
- 只获取文本、JSON、MediaWiki/API 响应或固定 Git 文件；不获取专有地图瓦片、图片、视频、大型压缩包，不探测私有接口；
- 每秒最多 1 个请求，单响应不超过 8 MiB，单任务累计不超过 32 MiB，网络吞吐不超过 1 MiB/s；
- 使用明确 User-Agent、超时、有限重试和缓存；遇到 403、429、许可不明或结构漂移立即停止该来源；
- 原始响应先写入不可变 Source Snapshot，并记录 URL、获取时间、HTTP 元数据、内容哈希和许可证；规范数据只能从 snapshot 生成；
- 同时仅一个获取或规范化工作者，不启动浏览器 GUI，不渲染地图，不使用 GPU；
- MapGenie、Fextralife、Wand、AtlasForge、IGN、Game8 等专有产品只允许人工功能核对，未经许可不得批量抓取瓦片、坐标或私有 API。

## 4. `SNAPSHOT_IO` 通道

只有满足全部条件后，才允许游戏运行时解析真实游戏数据：

1. 快照位于与游戏 C: 不同的健康物理磁盘，而不只是不同盘符；
2. 目标盘已核对 DiskNumber、BusType、型号、健康状态和剩余空间；
3. 当前历史上不稳定的 D: 明确禁止作为快照源、快照目标、解包目录或日志目录；
4. 快照在游戏未运行时制作，并与 `data/source-manifest.json` 的 BHD/Regulation 哈希闭环；
5. 快照目录只读，解析结果写到同一独立物理盘的工作区，避免回写 C:；
6. 解析器支持分块、断点续跑、原子提交和资源限额；
7. 不启动 GUI，不使用 GPU，不触碰原始游戏安装和存档。

建议容量：

- 原始归档快照约 65 GiB；
- 解包和中间产物按工具策略预留 80–200 GiB；
- 在盘面容量、健康和物理独立性未确认前，不开始复制。

### 4.1 快照制作

快照制作属于 `IDLE_ONLY`：

1. 确认游戏、EAC 和启动器进程均未运行；
2. 复核源文件哈希和 Steam BuildID；
3. 选择健康、非 D:、与 C: 物理独立的目标盘；
4. 只复制 BHD/BDT、Regulation 和必要的小型元数据；
5. 对复制后的 BHD、Regulation 和抽样 BDT 分块做校验；如需全量 BDT 哈希，在空闲时完成；
6. 将快照标记为只读，并写入独立 snapshot manifest；
7. 后续游戏运行期间只读快照，不再读取 `C:\MyProgram\Steam\steamapps\common\ELDEN RING\Game`。

## 5. 资源治理参数

`SNAPSHOT_IO` 默认预算：

| 资源 | 默认上限 |
|---|---:|
| 并发工作进程 | 1 |
| 进程优先级 | BelowNormal；解析块可降至 Idle |
| CPU 总占用目标 | 平均不高于 8%，5 秒峰值不高于 15% |
| 工作集 | 不高于 `min(1536 MiB, 物理内存 8%)` |
| 快照盘顺序读取 | 不高于 4 MiB/s |
| 快照盘写入 | 不高于 2 MiB/s |
| 单个原子处理块 | 不高于 64 MiB 或 20 秒，先到者为准 |
| GPU | 0；禁止硬件加速渲染、纹理解码和计算 |
| 后台下载 | 不高于 1 MiB/s；大型下载归入 `IDLE_ONLY` |
| 日志刷新 | 最多每 10 秒一次，批量追加，不逐条同步落盘 |

这些参数是初始安全值，不是性能目标。只要用户感觉到卡顿、音频异常、加载延迟或网络波动，就应继续下调或完全暂停，而不是以“平均指标正常”否定实际体验。

## 6. 自动停让与静默要求

调度器只通过普通进程枚举判断游戏是否运行，不打开进程句柄做内存读取。

状态规则：

1. 检测到 `eldenring.exe` 后立即进入 `GAMING_SAFE + ONLINE_INGEST`；
2. 没有独立快照时，所有归档任务保持排队；
3. 有合格快照时，只允许 `SNAPSHOT_IO` 限速任务；
4. 可用内存低于 20%、项目进程越过 CPU/内存预算、独立盘延迟持续升高或用户暂停标记存在时，停止调度新块；
5. 游戏退出后等待 120 秒冷却，再允许 `IDLE_ONLY`；
6. 任何任务退出码非零时只写项目日志并暂停该队列，不弹窗、不重试风暴；
7. 不注册开机启动、计划任务、全局热键或系统托盘程序，除非用户以后单独授权。

## 7. 分块与恢复

每个后台工作项必须记录：

- `task_id`；
- 执行通道；
- 输入文件及其 hash/build；
- 输入/输出物理盘；
- 预计 CPU、内存、磁盘和持续时间；
- 当前分块和检查点；
- 原子输出临时名与完成后的最终名；
- 退出原因、重试次数和最后错误。

任务只能在一个完整分块成功后推进检查点。暂停或游戏状态改变时，未完成临时块可以删除或重做，但不得把半成品发布到 Canonical Dataset。

## 8. 各阶段在游戏运行时能做什么

| 阶段 | `GAMING_SAFE` | `ONLINE_INGEST` | `SNAPSHOT_IO` | `IDLE_ONLY` |
|---|---|---|---|---|
| Phase 0 | manifest/许可/Schema/工具审计 | 来源元数据与许可证快照 | 不需要 | 制作快照、安装/编译工具 |
| Phase 1A | 在线模型、测试和证据模板 | 全成就、赐福、地区、入口、路线语义和关键状态 | 不需要 | 可选人工/浏览器核对，但不得在游戏运行时启动 |
| Phase 1B | 四类本地样片契约 | 在线样片差异补证 | 从独立快照限速提取 MSB/FMG/Param 小样 | 直接游戏归档读取、Smithbox、实机核对 |
| Phase 2–4 | 地图清单规则、去重、关系审计和差异队列 | 分批扩展合法在线 POI 和说明 | 分块生成本地候选数据 | 大规模全量解包、纹理处理 |
| Phase 5 | RouteNode/Transition/guard/effect、路线测试 | 获取并交叉核对路线证据 | 分块读取碰撞/Navmesh/事件候选 | 大规模图生成与实机路线验证 |
| Phase 6 | UI 代码审阅和小型单测 | 小型增量数据同步 | 不需要 | GPU 地图渲染、浏览器 E2E、性能压测 |
| Phase 7 | 存档 Schema 和隐私测试夹具 | 不需要 | 不需要 | 真实存档导入、离线位置采集 |
| Phase 8 | 差异规则和发布文档 | 在线 revision 差异快照 | 从新快照限速生成 diff | 新版本快照、全量回归和发布构建 |

## 9. 并行模式验收标准

正式宣称“可边玩边执行”前必须通过：

- 运行期间没有 GUI、UAC、Toast、声音、焦点变化或全局热键；
- 游戏 PID/启动时间未被本项目改变；
- 游戏目录和存档写入计数为 0；
- 未打开用于读写内存、调试或注入的游戏进程句柄；
- 没有合格快照时，游戏运行期间游戏盘 BDT 读取量为 0；
- 在线同步符合来源注册表和 1 req/s、8 MiB/response、32 MiB/task、1 MiB/s 上限，且没有瓦片、图片或视频请求；
- 所有后台进程都受并发、CPU、内存、磁盘、GPU和网络预算约束；
- 强制暂停标记能在一个原子块内生效；
- 工作者崩溃只生成日志，不弹窗、不自动无限重启；
- 先分别完成 30 分钟 `GAMING_SAFE` 与 `ONLINE_INGEST` 静默试运行，再单独验收 `SNAPSHOT_IO`；
- 用户实际游玩无可感知掉帧、卡顿、音频异常、加载异常和网络波动；用户体验优先于后台统计。

## 10. 当前主机的实际执行决定

- 游戏与项目当前都位于 C:，因此直接读取游戏归档不能在游戏运行时执行；
- D: 有历史不稳定记录，禁止承载本项目任何快照或中间数据；
- 尚未核实 E:/S:/I:/J:/L: 是否为健康且独立的物理磁盘，不能按盘符猜测；
- 当前执行 `GAMING_SAFE + ONLINE_INGEST`：文档、Schema、来源审计、小型许可数据快照、规范化、小型验证和 Git 提交；
- Phase 1A 在线 V1 立即推进；Phase 1B 本地真值提取保留在队列中，若后续找到合格独立盘则启用 `SNAPSHOT_IO`，否则只在游戏退出后的空闲窗口执行。

## 11. 实施顺序

1. 将本方案纳入主执行规格并建立机器可读 execution policy；
2. 在游戏运行时完成 Schema、来源台账、在线 Source Snapshot、规范化、解析器接口和小型测试设计；
3. 等游戏退出后，低负载核实可用物理盘身份、健康和空间；
4. 若存在合格独立盘，制作并校验只读快照；
5. 实现单工作者、隐藏窗口、资源限额、检查点和暂停标记；
6. 依次验收 `GAMING_SAFE`、`ONLINE_INGEST` 和 `SNAPSHOT_IO`；
7. `ONLINE_INGEST` 出现可感知网络/帧时间干扰时立即停用并退回纯 `GAMING_SAFE`；本地归档始终保持 idle-only，直到 `SNAPSHOT_IO` 独立通过验收。

---

**最终原则：项目可以与游戏并行，但游戏永远拥有资源优先权。逻辑隔离靠不接触进程和存档，性能隔离靠独立物理盘、限速、单工作者和自动停让；缺少任一条件时，不得声称互不干扰。**
