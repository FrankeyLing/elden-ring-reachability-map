# 《艾尔登法环》全位面 1:1 互动地图执行规格

> 文档版本：1.2
> 编制日期：2026-08-17（Asia/Shanghai）
> 本次修订：采用 Online-first V1；先用有许可、可追溯的在线结构化数据交付真实可用版本，再由本地游戏文件校准为完整 1:1
> 目标目录：`C:\Users\Frankey\ZCodeProject\repos\elden-ring-reachability-map`
> 当前阶段：Phase 1A Online-first V1；允许在 `GAMING_SAFE + ONLINE_INGEST` 约束内进行在线数据接入和产品实现，禁止影响前台游戏。

## 0. 执行结论

本项目不能继续把现有 `data/graph.json` 当作《艾尔登法环》真实地图逐步“补全”。正确做法是：

1. 保留现有界面中可用的交互概念，例如筛选、起终点、路线步骤和节点详情；
2. 将现有演示数据整体隔离为 `demo fixture`，任何节点和边都不得未经重新取证进入正式数据集；
3. V1 优先从 Eldenpedia、ERDB、FrontierNav，以及经逐文件许可/来源审计的 Map For Goblins 和 `er-guide` 建立在线数据快照，先交付真实 POI、全成就导航、赐福到目标路线和关键世界状态；
4. V1 每条记录必须标明在线来源、revision/commit、许可证、抓取时间、坐标类型和证据等级；没有来源的数据不得进入正式构建；
5. V2 以用户合法持有的当前 PC 游戏安装文件作为坐标、地图实体、物品、事件和本地化名称的最终主真值，对 V1 逐项升级为 `local_game_verified`；
6. “所有位面”必须覆盖地表、地下、幽影之地、全部可游玩的独立地图、遗迹地牢的每个实际楼层、Boss 独立场景和世界状态变体；
7. 知识图谱中的“属于、位于、掉落、对应地图”等关系必须与“玩家能从 A 到 B”彻底隔离，只有带方向、条件、几何和证据的正式 `Transition` 才能进入寻路器；
8. MapGenie、Fextralife、Wand、AtlasForge 等专有互动地图只做人工查漏和 UX 对标，不绕过 robots、不抓私有接口、不镜像瓦片或批量复制专有坐标；
9. 公开发布若要直接展示游戏原始地图纹理、图标或模型，必须先取得相应权利人的书面许可；未获许可时，V1 使用自制/抽象底图，V2 公开版继续使用自制视觉层。

项目先交付标明来源与在线证据等级的 `Online Verified V1`，解决“目标在哪、从哪个赐福出发、入口和楼层在哪里、当前状态能不能拿到”；再通过本地游戏文件升级为能够声明空间、数据和拓扑 1:1 的最终版本。

## 1. 现有原型审计

### 1.1 已确认现状

- 当前目录已初始化 Git；Phase 0 环境基线已有独立提交，现有未跟踪 WebUI 演示文件继续保持隔离，不纳入正式数据提交。
- README 已明确说明现有数据只是演示拓扑，不是完整世界数据集。
- 当前数据只有 25 个节点、30 条边、5 个条件和 4 个粗粒度图层。
- 所有坐标都位于人为设定的 `1000 × 600` SVG 画布，不是游戏世界坐标。
- 时间和风险值是演示值，没有测量方法、版本或证据。
- 当前路由只是在演示边上运行 Dijkstra；算法能运行不代表边是真实存在的。

### 1.2 已发现的明确虚构或错误关系

- 神授桥直接传送到圣树树冠；
- 亚坛高原直接传送到逐渐崩毁的法姆·亚兹拉；
- 风暴山丘通过灵马气流直达亚坛高原；
- 王城下水道中央隐藏传送到神授桥；
- 学院传送门被简化成直达迪可达斯大升降机；
- “击败蒙葛特后才能从大道旁露台去黄金树大教堂上层”的条件方向构成剧情逻辑错误。

这些边即使标注了“演示”，也会污染后续数据和验收，因此正式项目不得做增量修补。

### 1.3 处置决定

| 现有部分 | 处置 |
|---|---|
| `data/graph.json` | 整体隔离为演示夹具；正式数据禁止继承 |
| 当前 SVG 坐标 | 废弃 |
| 当前时间、风险、条件 | 废弃 |
| Dijkstra 实现 | 仅可保留为算法单测参考，不作为正式路由设计 |
| 筛选、节点详情、路线步骤等 UX | 作为需求参考，后续重新接入真实数据 |
| Python 静态服务器 | 仅供原型查看，不预设为最终架构 |

## 2. “1:1”和“所有位面”的验收定义

### 2.1 1:1 不等于只复制游戏地图图片

本项目把 1:1 拆成四个可独立验收的维度：

| 维度 | 必须达到的含义 |
|---|---|
| 空间 1:1 | 保存游戏原始 `map_id + X/Y/Z`，所有显示坐标均由有版本的变换生成，不手工目测摆点 |
| 数据 1:1 | 物品、敌人、Boss、NPC、赐福、入口、传送、事件状态与当前目标游戏版本一致 |
| 拓扑与可达性 1:1 | 上下楼、单向跳落、梯子、升降梯、气流、传送、门、雾门和剧情封锁方向正确；路线只使用当前玩家状态下真正开放的边 |
| 视觉 1:1 | 私有本地版可从用户自己的安装生成原始美术；公开版是否能使用原始美术取决于书面授权 |

任何一个维度未达标，产品都不得宣传为“完整 1:1”。

点位多、分类多、实时显示玩家坐标或二维距离很近，都不能证明可达性正确。若系统不能解释入口、楼层、连接方式、方向和前置状态，即使点位全部存在，也只能叫 POI 地图，不能叫本规格中的完整互动地图。

### 2.2 目标版本

- `1.16.1` 只能作为此前在线资料的历史参考，不能继续当作本机生产真值。
- 当前本机安装已核实：Steam AppID `1245620`、BuildID `22984413`、`eldenring.exe` 文件/产品版本 `2.6.2.0`；完整哈希和路径见 `data/source-manifest.json`。
- 当前 `regulation.bin` 已取得哈希，但内部 Regulation 版本尚未由标题画面或固定解析器确认；在确认前不得把 `2.6.2.0` 直接写成 Regulation 版本。
- 基础内容：本体 + `Shadow of the Erdtree`。
- `Tarnished Pack` 官方计划于 2026-08-28 发布，距本文档日期仅 11 天。官方尚未声明其一定不改地图相关数据，因此：
  - 可以先用当前本机安装完成提取与坐标链路的技术验证；
  - 不得在 Regulation 版本和后续内容基线未确认前冻结最终生产数据；
  - 新内容发布后必须重新哈希、重新提取并生成版本差异报告；
  - 最终交付页必须显示实际提取的 App/Regulation 版本，不能只写“最新版”。

`ELDEN RING NIGHTREIGN` 是另一款游戏，不在本项目范围内。

### 2.3 必须覆盖的地图空间

1. 交界地地表：所有实际承载可交互实体的基础世界大/中/小瓦片；
2. 交界地地下：希芙拉河、诺克隆、安瑟尔河、诺克史黛拉、腐败湖、深根底层、蒙格温王朝等全部连续区和独立 Boss 区；
3. 幽影之地：墓地平原、幽影亚坛、南方海岸、劳弗、谷底/深渊、尖刺山及其全部连续高低层；
4. 大型遗迹与独立区域：史东薇尔、魔法学院、王城、灰烬王城、火山官邸、法姆·亚兹拉、圣树、圆桌厅堂、神授塔、地下大道等；
5. 全部小型地牢：洞窟、坑道、地下墓地、英雄墓地、监牢、熔炉、陵墓、竞技场等；
6. 全部可游玩独立场景：教程区、传送孤岛、Boss 独立场地、结局前可操作空间等；
7. 世界状态变体：至少包含击败“黑剑”玛利喀斯前的王城罗德尔与击败后的灰烬王城，以及其他会改变几何、实体或可达性的状态；
8. 每张独立地图内部的真实楼层：地面、地下、屋顶、夹层、升降梯端点、洞穴上下层均须可切换。

不可游玩的天空盒、纯过场、测试和废弃地图也要进入“地图审计总表”，但默认不显示。每项必须标明 `playable / cutscene / unused / duplicate / unknown`，不能静默丢弃。

### 2.4 状态维度不能压成一个布尔值

路线计算至少要同时区分四类状态：

| 状态 | 含义 | 示例 |
|---|---|---|
| `world_epoch` | 会替换地图、几何或大范围实体的持久世界纪元 | `leyndell_royal_pre_maliketh`、`leyndell_ashen_post_maliketh` |
| `progression_snapshot` | 周目、Boss、任务、物品、钥匙和已激活赐福等玩家进度 | 已击败 Boss、石剑钥匙数量、传送门是否激活 |
| `mechanism_state` | 当前地图内可改变但不一定永久保存的机关状态 | 升降梯在上端/下端、门已开、教区水位、陷阱位置 |
| `route_profile` | 玩家允许采用的通行方式 | 正常流程、技巧跳跃、速通漏洞、禁快速旅行 |

同一地图不同纪元不能叠成一张假地图；未知状态默认采用保守策略，不得把“可能开放”当成“已经可走”。若某条路线依赖用户未提供的状态，结果必须明确列出假设或返回“当前状态无法证明可达”。

## 3. 数据源分级与接入策略

### 3.1 权威顺序

V1 的实施优先级是：可许可接入的在线结构化数据 → 在线路线/知识图谱 → 专有互动地图人工核对 → 本地游戏文件后续校准。这样可以先交付产品，不必等待本地归档提取。

但出现事实冲突时，权威顺序仍然是：

1. 当前目标版本的游戏文件与实际离线游戏行为；
2. 官方版本公告和游戏内文本；
3. 可复现的开源格式解析结果；
4. 带版本和修订号的 Wiki/社区结构化资料；
5. 在线互动地图和攻略文章；
6. 无法追溯版本的搜索摘要、帖子或二次转载。

低等级来源可以触发复查，不能直接覆盖高等级来源。V1 尚无本地证据的记录必须保留 `online_single` 或 `online_cross_checked` 状态；V2 才能升级为 `local_game_verified`。

### 3.2 具体来源矩阵

| 来源 | 主要用途 | 接入方式 | 约束与结论 |
|---|---|---|---|
| 用户合法持有的游戏安装 | 地图、坐标、实体、参数、事件、文本、纹理主真值 | 本机只读提取并哈希 | 原始资源不进入公开源码仓库 |
| Bandai Namco / FromSoftware 官方页面 | 版本、DLC 范围、发布日期、许可边界 | 保存 URL、发布日期和页面快照哈希 | 1.16.1 是历史参考；当前本机 EXE 为 2.6.2.0，Regulation 仍需单独确认，8 月 28 日后必须重基线 |
| Smithbox | 可视化核对 MSB、模型、纹理、参数、世界瓦片与 Navmesh | 固定到明确发行版/提交；人工与自动提取双重核对 | 当前发行页显示 2.2.4；工具版本不得静默漂移 |
| SoulsFormatsNEXT + Paramdex | 解析 BND/DCX/MSB/PARAM 等格式及字段定义 | 固定提交，记录许可证和 DataVersion | 字段定义变化必须让构建失败，而不是继续生成垃圾数据 |
| Souls Modding Map Overview | 地图 ID、瓦片层级、地图连接和名称审计 | 作为带修订信息的交叉校验清单 | 该页说明 m60/m61 的嵌套瓦片和 `WorldMapLegacyConvParam`；不是替代游戏文件 |
| Eldenpedia / wiki.gg | 地区层级、描述、路线语义、攻略提示、来源引用 | 优先使用 MediaWiki 修订/API；保存页面 revision、URL、许可 | 页面文本通常为 CC BY-SA 4.0；图片需逐项核权；其待办页仍列出 DLC 缺口，不能作为完整性真值 |
| Map For Goblins DLL | 物品、事件奖励、赐福、气流、雕像等提取逻辑的独立审计 | 审阅 MIT-style 代码和生成方法；比较统计和差异 | 其 vanilla + DLC 构建约有 6700 图标，可作规模参照，不能直接复制生成的游戏资产 |
| ERDB | 物品数据和地图提取方法参考 | 优先本地运行并指定版本；远程 API 仅作抽样 | MIT 代码可评估，但最新 GitHub release 为 2023 年，不能假定覆盖 DLC/当前版本 |
| Elden Ring Compass | 游戏安装提取、只读存档解析、连续存档轮询和 Web 地图设计参考 | 固定当前提交做隔离 spike；逐项审计数据来源、测试与许可证后再决定复用范围 | 当前 README 已说明 extractor 可解析 FMG/PARAM/MSB/EMEVD 等内容，不能再标成 stub；但“实现可用”不等于已获准直接并入本项目 |
| FrontierNav | 知识图谱、实体/关系模式、Deep Query 和地图/Wiki 一体化架构参考 | 只参考公开文档和经许可的数据；记录页面 revision 与访问时间 | 图关系是语义关系，不天然是通行关系；公开地图编辑文档当前仍以 Point feature 为主，不能当《艾尔登法环》现成路线图或当前版本真值 |
| Elden Ring 100% Route Guide (`aether-auto/er-guide`) | “地区 → 赐福到赐福 leg → 步骤”的路线写作、覆盖检查和本地进度 UX 参考 | 审阅 MIT 代码与手写路线结构；只导入自行验证且许可允许的抽象 | 路线由人工编写且明确不绘制实际 polyline；其第三方瓦片/坐标不得直接复制，不能替代状态化寻路图 |
| ER Route Tracker | 离线实机 `map_id + X/Y/Z`、轨迹、跨图点和路线录像的采集方法参考 | 仅在隔离、离线、关闭 EAC 的 QA 环境评估；优先从源码构建并做安全/许可证审计 | 当前为 Alpha，涉及注入和 EAC bypass；只可作为证据采集器，不能作为线上依赖或自动寻路真值 |
| Wand | 地表/地下/幽影之地实时位置、附近点提醒和清单交互参考 | 人工功能核对，不抓取私有接口或专有数据 | 实时位置只能提供路线起点，不能证明悬崖、楼层和入口之间可达 |
| AtlasForge | 三大顶层地图、POI 分类、分层和 3D 浏览交互参考 | 人工功能核对，不复制瓦片、坐标或专有数据 | 仍以兴趣点和视觉层为核心，不作为通行边来源 |
| MapGenie、Fextralife、IGN、Game8 等在线地图 | 发现漏点、名称差异和用户常用分类 | 人工对照或取得书面许可后的正式接口/导出 | 禁止绕过 robots、批量镜像瓦片或抓取私有接口；不得把第三方坐标当主真值 |
| Future Press 等攻略书 | 特殊路线与官方攻略语义复核 | 使用合法购买版本人工核对 | 不扫描、复制或公开再分发受版权保护的页面 |

不存在一个可以单独宣称完成全世界 1:1 的上游，但 V1 可以组合多个在线源直接交付。具体接入许可、用途和排除项以 `data/online-source-registry.json` 与 `ONLINE_FIRST_V1_PLAN.md` 为准；本地游戏文件与实机行为继续作为后续最终裁决依据。

### 3.3 外部来源留痕

每一条外部补充都必须带：

- `source_url`；
- 页面/提交/数据版本；
- 获取时间；
- 许可证或使用限制；
- 原始标题；
- 内容哈希；
- 它影响了哪个字段；
- 是否已经被游戏文件或实际游玩验证。

若来源以后失效，已有数据仍能说明当时依据，且不会被误标成官方事实。

## 4. 启动前置条件与停止线

### 4.1 当前已知前置状态

- 已发现并核实 Steam 安装路径：`C:\MyProgram\Steam`。
- 已发现真实游戏目录：`C:\MyProgram\Steam\steamapps\common\ELDEN RING\Game`。
- `eldenring.exe` 文件版本为 `2.6.2.0`；Steam BuildID 为 `22984413`。
- `regulation.bin`、EXE、Data0/1/2/3 BHD 和 DLC BHD 的 SHA-256 已记录在 `data/source-manifest.json`。
- 当前游戏进程正在运行；Phase 1A 可在限速、无 GUI 的在线数据通道中继续，直接游戏归档提取、工具启动和实机采集延后到 Phase 1B 的合格窗口。

### 4.2 开工前必须满足

1. 复核一份合法 PC 安装路径；当前已确认 `C:\MyProgram\Steam\steamapps\common\ELDEN RING`；
2. 在标题画面或固定解析器确认 App/Regulation 版本，并对关键文件建立 SHA-256 清单；当前 EXE/Steam manifest 已记录，Regulation 仍待确认；
3. 确认本地私用版还是拟公开部署版。默认先做本地私用真值版；
4. 建立 Git 仓库，但把原始游戏资源、解包缓存和存档加入忽略规则；
5. 确认剩余磁盘空间和备份位置；
6. 整个提取过程只读，不修改游戏安装，不把修改器接入在线/EAC 会话；
7. 游戏运行期间允许按 `CONCURRENT_EXECUTION_PLAN.md` 执行有许可、可追溯的小型文本/JSON 在线快照与规范化；直接读取原始游戏 BDT、制作快照、大规模解包、GUI 工具和实机采集仍属 `IDLE_ONLY`，只有来自已验证独立健康物理盘的只读快照才可进入 `SNAPSHOT_IO`；任何通道均不得触碰游戏进程、存档或前台窗口。

### 4.3 必须停止而不能猜的情况

- 游戏文件版本与官方/标题画面版本不一致；
- Param DataVersion 或 MSB 格式无法被已固定工具正确解释；
- 某个 dungeon-to-world 变换无法闭环验证；
- 同一实体在静态文件与实际游戏中状态冲突；
- 第三方来源无许可证却需要批量复制；
- 原始美术拟公开发布但尚无授权；
- 游戏进程正在运行而操作计划需要读取大型归档、启动工具或采集实时位置；
- 关键路线只能靠“看起来应该能走”推断，尚未实机验证。

### 4.4 游戏并行执行模式

并行工作的完整硬边界见 `CONCURRENT_EXECUTION_PLAN.md`，机器可读约束见 `data/execution-policy.json`：

- 当前游戏运行时启用 `GAMING_SAFE + ONLINE_INGEST`，用于文档、Schema、许可证审计、受限文本/JSON 获取、Source Snapshot、规范化和小型校验；
- 在线任务单响应不超过 8 MiB、单任务累计不超过 32 MiB、每秒不超过 1 个请求、吞吐不超过 1 MiB/s，只允许文本/JSON，不获取地图瓦片、图片、视频或大型压缩包；
- 在线结果必须先落到不可变 Source Snapshot，再由 snapshot 构建规范数据；页面改版或接口失效不能直接污染已发布数据；
- 当前游戏与项目都在 C:，且没有合格独立快照盘，因此直接本地归档任务全部排队；历史不稳定的 D: 不得用作快照、缓存、日志或中间数据盘；
- 任务必须无窗口、无通知、无声音、单工作者且失败静默；绝不向 `eldenring.exe`、EAC、Steam 游戏窗口或存档发出任何操作。

## 5. 数据生产架构

正式数据分四层，任何层都不能跳过：

1. **Source Snapshot**：游戏文件哈希、外部页面 revision、工具提交和许可证；
2. **Raw Extract**：保持游戏原始 ID、字段、浮点坐标和引用，不做业务解释；
3. **Canonical Dataset**：统一地图、楼层、实体、状态、连接、名称和证据模型；
4. **Published Dataset**：只包含通过许可与质量门的数据，并针对 Web 做瓦片、索引和压缩。

### 5.1 核心实体模型

| 实体 | 必备字段 |
|---|---|
| DatasetVersion | App/Regulation 版本、DLC、平台、语言、关键文件哈希、生成器提交 |
| Plane | 交界地地表、交界地地下、幽影之地、独立区域等顶层空间 |
| Map | 原始 map ID、类型、父区域、可游玩状态、世界状态变体、边界 |
| Floor | map ID、楼层 ID、拓扑顺序、高度范围、可见几何、入口地标 |
| Entity | 稳定 ID、原始实体 ID、类型、原始坐标、显示坐标、状态、名称 ID |
| WorldEpoch | 纪元 ID、适用地图、进入/退出 flag、被替换几何与实体、互斥纪元 |
| StateSnapshot | 周目、世界纪元、Boss/任务 flag、钥匙与关键物、已激活赐福、机关状态、未知字段 |
| RouteNode | 节点类型、map/floor、精确坐标、连通分量、可作为起点/终点/中继的权限 |
| SemanticRelation | 起终实体、关系类型、来源、版本；必须固定为 `routeable=false` |
| Transition | 路线节点起终点、方向、移动方式、guard、effect、几何、代价、证据、验证状态、`routeable=true` |
| RouteRequest | 起点、目标、StateSnapshot、路线 profile、未知状态策略和优化目标 |
| RouteResult | 采用的状态/假设、起始赐福、节点序列、必经连接、锁定原因、距离/时间/风险和证据 |
| ItemPlacement | ItemLot、物品/数量、容器/尸体/敌人/事件来源、收集状态 flag |
| Encounter | NPC/敌人/Boss 参数、生成条件、死亡/完成 flag、奖励 |
| SourceEvidence | 来源、版本、字段、证据等级、复核人、复核时间 |
| Discrepancy | 冲突双方、严重度、结论、处理依据、是否阻塞发布 |

稳定 ID 不能使用网页坐标或中文名称拼接。优先由 `game_version + map_id + entity_id + lot/event id` 派生，并保留版本迁移表。

### 5.2 证据等级

- **A**：游戏文件直接取值，且实机/渲染抽样确认；
- **B**：游戏文件直接取值，尚未逐点实机确认，但引用链完整；
- **C**：两个以上独立社区来源一致，游戏文件尚未定位；
- **D**：单一社区说法或人工推测。

公开默认图层只展示 A/B。C 必须显示“待实机确认”；D 不进入正式地图。

### 5.3 知识图谱与导航图必须硬隔离

系统同时可以拥有知识图谱和导航图，但两者不是同一个图：

- `G_K = (V, R)`：知识图谱，`R` 可以表达“属于地区、位于地图、掉落物品、任务需要、地图标记指向实体”等语义；
- `G_T = (V_T, E_T)`：基础通行图，`E_T` 只能是已经建模并取证的 `Transition`；
- `G_T(s) = (V_T, {e ∈ E_T | guard_e(s) = true})`：给定玩家状态 `s` 后真正交给寻路器的可用图。

必须执行以下不变量：

1. `MapFeature-MAP_TARGET`、`Area-REGION`、`located_in`、`drops`、`requires` 等关系永远不能直接进入 `E_T`；
2. “两个地图有关联”“两个点二维距离很近”“一个点属于另一个区域”都不能自动生成通行边；
3. 第三方资料描述了传送、门或路线时，只能创建 `TransitionCandidate`，补齐游戏文件/实机证据后才能晋升为正式 `Transition`；
4. 图编译器若发现 `SemanticRelation` 被路由索引引用，必须构建失败；
5. Deep Query 可以跨语义关系检索资料，但自动寻路只能运行在 `G_T(s)` 上。

### 5.4 路线节点分类

正式 `RouteNode` 至少包含：

- 赐福及其实际可站立锚点；
- 道路/房间/洞穴关键岔路；
- 建筑、洞窟、井、排水口、屋顶和地图边界入口；
- 门、雾门和封印的两侧；
- 梯子、升降梯和升降平台的每一个端点；
- 跳跃起点、单向落点和安全着陆点；
- 传送门、棺材、气流、送返门等来源端与目标端；
- Boss 房入口/出口及战后开放端；
- 仅作为最终目标的物品、NPC、Boss 或事件锚点。

一个普通 POI 默认只是 `Entity` 或终点锚点，不得因为它有坐标就成为可穿越的中继节点。门、电梯、梯子和传送的两端必须分别建节点；单向落点不得复用为反向起点。

当前位置只能吸附到“同一 map、同一 floor、同一已验证可行走连通分量”中的节点或路径。禁止只按二维欧氏距离选择最近节点；无法安全吸附时，系统必须要求用户改选一个已知赐福或明确入口。

## 6. 坐标、地图瓦片和楼层方法

### 6.1 坐标不可丢失

游戏原始向量按 `(X, Y, Z)` 保存，其中二维俯视平面使用 `(X, Z)`，`Y` 作为高度。正式数据同时保存：

- 原始局部坐标；
- 原始 map ID；
- 归一化世界坐标；
- 地图 UI 像素/瓦片坐标；
- 使用的变换矩阵及其版本；
- 正反变换误差。

前端坐标永远由变换生成，不允许运营人员拖动后覆盖原始坐标。确需视觉偏移时，另存 `label_offset` 或 `cluster_offset`，并明确它不是实体坐标。

### 6.2 开放世界瓦片

Souls Modding 的地图资料说明：

- 基础世界和 DLC 开放世界使用 m60/m61 系列嵌套瓦片；
- 大、中、小三层瓦片共同加载；
- 小瓦片约覆盖 `256 × 256` 游戏单位；
- 相邻小瓦片中心相差 256 单位；
- 瓦片坐标从西南向东北增长。

这些规则用于建立开放世界统一坐标，但最终瓦片清单必须从目标安装实际枚举，不能把社区文档中的列表硬编码为真值。

### 6.3 游戏菜单地图美术

已公开的地图拼装技术资料显示，本体地图由 256 像素小块和多个缩放等级组成，表面与地下有不同图组，且同一坐标可能因地图碎片状态存在多个纹理版本和废弃块。

执行要求：

1. 自动枚举当前安装中的图组、缩放级别、坐标和状态掩码；
2. 不把 2022 年资料中的固定范围当成 DLC 的永久规则；
3. 通过实际 `WorldMapPoint` 控制点判定正确图块，不能只按文件名挑最大掩码；
4. 保存“已揭图”和“未揭图”版本，但默认公开完整已揭图；
5. 废弃/错位图块进入审计报告，不参与拼图；
6. 原始拼图只在本地或已授权环境输出。

### 6.4 独立地图到世界地图的转换

- 优先读取 `WorldMapLegacyConvParam` 等游戏参数给出的偏移、旋转和缩放；
- 再用赐福、入口、地图标记和连接碰撞作为控制点；
- 正向投影和反向投影必须闭环；
- 一个地牢若没有可靠世界投影，仍保留自己的局部精确坐标，不得用目测位置伪装成统一世界坐标。

### 6.5 楼层生成

楼层不能简单按高度等距切片。史东薇尔、王城、法姆·亚兹拉、圣树、幽影城和劳弗均有坡道、洞穴、重叠建筑和长距离垂直连接。

正式方法是：

1. 从碰撞、可见几何和 Navmesh 构建可行走连通分量；
2. 识别门、梯子、升降梯、跳落、气流和传送等跨分量连接；
3. 结合入口、赐福、命名区域和实际路线，将连通分量聚合为用户可理解楼层；
4. 每层保存自己的可见多边形和遮罩；
5. 斜坡仍属于同一连通层，不因跨过一个高度阈值被错误切开；
6. 楼层切换处显示上下方向、入口地标、通行条件和是否可逆。

## 7. 地图渲染方案

### 7.1 两条视觉产线

**本地真值版**

- 从用户自己的安装生成游戏菜单地图和必要的本地几何渲染；
- 原始资源不上传、不提交、不分发；
- 可用于坐标校准和内部验收。

**公开可部署版**

- 若已取得书面授权，可使用许可范围内的原始地图美术；
- 若未取得授权，使用根据碰撞/Navmesh 生成并人工美术化的自制底图；
- 仍保留精确坐标、楼层、POI 和路线；
- 不从 MapGenie、Fextralife 或 IGN 镜像瓦片。

### 7.2 推荐前端渲染能力

正式实现阶段优先验证 deck.gl 的 Cartesian `OrthographicView + TileLayer`：它可以直接使用非地理 XY 坐标、按需加载瓦片并承载大量图标、文字、路径和楼层遮罩。需要立体核对时增加独立的 3D/爆炸视图，不让 3D 视角取代清晰的 2D 导航。

产物应为版本化的 raster/vector tile 金字塔；线上可评估单文件 PMTiles 或等效范围请求方案，离线包则提供完整本地瓦片。最终选型必须由性能样片决定，不在本文件中把某个框架写死。

## 8. POI 和游戏状态提取

### 8.1 主要数据链

| 数据类型 | 位置/实体主来源 | 名称与属性 | 条件与完成状态 |
|---|---|---|---|
| 赐福、传送点 | MSB + `BonfireWarpParam` | FMG | 赐福/地图相关 event flag |
| 地图标记、碎片 | `WorldMapPointParam` + MSB | FMG + Goods | 地图碎片 flag |
| 固定拾取物 | MSB Treasure/Asset/Enemy | `ItemLotParam_map/enemy` + Equip/Goods Params + FMG | 容器/拾取 event flag |
| 脚本奖励 | EMEVD 奖励指令 | ItemLot/物品参数 + FMG | 事件完成 flag |
| Boss/敌人 | MSB Enemy/Generator + NPC Params | FMG/区域文本 | EMEVD 死亡、阶段和奖励 flag |
| NPC/商人 | MSB + ESD | Talk/Character FMG + Shop Params | 任务与世界状态 flag |
| 门、机关、升降梯 | MSB Asset/Event/Region | ObjAct/事件参数 | 解锁、操作、剧情 flag |
| 气流、雕像、召唤池 | MSB/WorldMapPoint/相关 Params | FMG/图标 | 激活或封印 flag |
| 地区与楼层 | Map ID、区域 volumes、碰撞、赐福 | PlaceName FMG | 世界状态变体 |

不能只读 `regulation.bin`。许多奖励、Boss 完成和 NPC 状态只在 EMEVD/ESD 与 event flag 链中成立。

### 8.2 必须支持的用户分类

- 地区、地牢、赐福、地图碎片和入口；
- Boss、字段 Boss、入侵者、重要敌人和召唤协力；
- NPC、商人、铁匠、教师和任务节点；
- 武器、盾牌、护符、护甲、法术、祷告、战灰、骨灰；
- 关键物品、任务物品、制作笔记、绘画、石剑钥匙门；
- 黄金种子、圣杯露滴、灵药露滴、记忆石、护符袋等成长物；
- 锻造石、墓地/灵依铃兰、幽影树碎片、灵灰等强化物；
- 圣甲虫、素材点、一次性敌人掉落、宝箱、尸体和脚本奖励；
- 升降梯、梯子、门、气流、跳落、棺材、传送门和其他跨层连接；
- 竞技场、封印监牢、漫步灵庙、神授塔、雕像、召唤池和复活雕像。

重复刷新的普通素材默认折叠，用户可单独开启；唯一物和推进节点默认优先。

### 8.3 去重和“幽灵点”处理

- 同一拾取物可能同时出现在 MSB、ItemLot 和 EMEVD 引用链中，只能展示一个规范实体；
- 两个宝物引用但游戏只发放最后一个的情况必须按实机结果处理；
- 敌人理论掉落与一次性保证掉落要分开；
- dummy、cut、不可达、埋在地形内或仅供剧情演出的实体默认排除；
- 每个排除项必须有 reason code，不能靠人工删 JSON 后失去证据。

### 8.4 中文名称

- 简体中文主名称优先来自当前安装的官方 FMG；
- 同时保留繁体中文、英文、日文和内部 ID，便于检索与消歧；
- Wiki 翻译不能覆盖官方 FMG，只能作为别名；
- 某语言为空时显示明确 fallback，不生成空标题或猜译。

## 9. 多源冲突处理规则

1. 坐标冲突：游戏原始坐标胜出；第三方差异进入 discrepancy；
2. 名称冲突：目标语言 FMG 胜出；Wiki 名称记录为别名；
3. 掉落冲突：EMEVD/ItemLot 引用链与实际领取结果胜出；
4. 楼层冲突：原始 Y 高度 + 可行走连通性 + 实机入口路线共同判定；
5. 路线冲突：实际可完成、可复现的路线胜出；
6. 世界状态冲突：明确标出版本和前置 flag，不把多个时态叠成一张假地图；
7. 在线地图多出一个点：先查 dummy、脚本奖励、随机掉落和旧版本，不能直接导入；
8. 在线地图少一个点：先确认分类/过滤逻辑，再检查当前版本新增内容；
9. 知识关系与路线冲突：语义关系只能帮助定位证据，不能证明通行；只有正式 `Transition` 可以参与路线；
10. 仍无法判定：保持 unknown 并阻塞对应“完整性”声明。

## 10. 可达性和路线系统

### 10.1 路由不是两点画直线

正式路线由物理通行、显式连接和推进状态共同组成：

- **物理通行图**：来自可行走表面、碰撞、Navmesh 和明确的跨层连接；
- **推进状态图**：来自门、Boss、任务、钥匙、机关、世界状态和传送激活条件。

两者编译为第 5.3 节定义的 `G_T(s)`。游戏 Navmesh 主要服务 AI，不能单独证明玩家可走；玩家能跳跃、骑灵马、攀梯、落下和使用传送，因此必须使用碰撞/Navmesh、显式连接、事件条件和实机证据的混合建模。

### 10.2 Transition 必须记录

- 稳定边 ID、适用 DatasetVersion 和适用世界纪元；
- 起点和终点 RouteNode、精确 `map_id + X/Y/Z`、楼层与连通分量；
- 有向或双向；
- 步行、骑马、跳跃、落下、梯子、升降梯、气流、门、传送、棺材等方式；
- AND/OR/NOT 前置条件表达式，而不是笼统的 `has_teleporter`；
- 周目、世界纪元、钥匙数量、Boss/任务 flag、机关状态、赐福激活和快速旅行状态；
- 执行动作及其状态效果，例如消耗钥匙、开门、召回升降梯、激活传送、击败 Boss 或切换世界纪元；
- 路段几何、距离和高度变化；
- 时间、战斗、资源消耗、失败风险和不可逆性组成的代价向量；
- 入口地标、反迷路提示和关键画面；
- 数据来源和实机验证记录；
- 是否属于常规玩法、技巧跳跃或明确 exploit。

一条边的规范形式为：

`e = (u, v, mode, guard, effect, cost, geometry, evidence)`

其中 `guard` 决定当前状态能否执行，`effect` 决定执行后状态如何变化。没有 `effect` 的模型无法正确规划“先拿钥匙再开门”“先启动电梯再返回”“先击败 Boss 再通过”等路线。

### 10.3 RouteRequest 输入契约

每次路线请求必须明确：

| 字段 | 要求 |
|---|---|
| `start` | 当前离线位置、用户选定 RouteNode，或已激活赐福；必须说明来源和精度 |
| `goal` | Entity、RouteNode、Boss/物品/事件目标或一组全成就目标 |
| `dataset_version` | 当前 App/Regulation/DLC 数据版本，不允许跨版本静默计算 |
| `state_snapshot` | 周目、世界纪元、Boss/任务 flag、关键物、钥匙、已激活赐福、机关状态和未知项 |
| `route_profile` | 正常流程、技巧跳跃、速通漏洞、禁快速旅行、仅已解锁区域等 |
| `objective` | 最短时间、最少战斗、最低风险、最少资源、全收集顺序或用户指定权重 |
| `unknown_policy` | 默认 `block`；只有用户明确允许时才可把未知状态列为假设生成候选路线 |

当前位置来自实时位置、存档轮询或人工点选时，只能决定 `start`，不能创建、删除或证明任何 `Transition`。当前位置无法吸附到同 map、同 floor、同连通分量时，路线请求必须失败并要求用户选择一个已知入口或已激活赐福。

### 10.4 状态随路线动作演化

只筛选一次初始状态还不够。若边会改变状态，规划器必须在 `(RouteNode, StateSnapshot)` 的状态空间中搜索，并逐步执行：

`s_{k+1} = apply(effect_e, s_k)`

至少要正确处理：

- 石剑钥匙等资源消耗后，后续门是否仍可开启；
- 门、升降梯、传送门、气流和水位等机关被激活后的新边；
- Boss 战作为路线动作时的死亡/击败状态和战后出口；
- 玛利喀斯等不可逆剧情节点导致的世界纪元切换和旧地图边失效；
- 任务分支互斥、NPC 移动和已经错过的物品；
- 快速旅行解锁、战斗中/地牢限制及不可传送状态。

规划器不得把相互排斥的世界纪元拼成一条路线，也不得通过循环条件“先经过锁门再取得开门所需物品”。

### 10.5 默认路线规则

- 默认只给无漏洞、正常流程可复现的路线；
- 技巧跳跃和速通 exploit 使用独立 profile，不混入普通路线；
- 快速旅行必须满足已发现赐福和当前可传送状态；
- 单向落差不能自动生成反向边；
- 两个平台几何上接近，不代表能跳过去；
- 语义关系、地图归属关系和 POI 邻近关系一律不得充当捷径；
- 未知 guard 默认视为不可用，并解释缺少哪项状态；
- “预计时间”只有在移动速度和动画耗时经过实测校准后才展示；
- “风险”是产品策略值，不是游戏事实，必须说明计算规则，未校准前不展示。

### 10.6 RouteResult 输出契约

所有路线必须先显示数据版本、世界纪元、路线 profile、状态来源、未知项和采用的假设，然后按以下固定骨架输出：

`最近可用且已激活的赐福/当前有效起点 → 拓扑节点 → 必经连接 → 单向或条件边 → Boss/物品/事件目标`

具体要求：

1. “最近赐福”按 `G_T(s)` 中的可达代价计算，不按二维直线距离计算；
2. 每一步输出起终节点、地图、楼层、入口地标、移动方式、方向、条件和是否可逆；
3. 经过门、电梯、梯子、传送、气流、棺材、Boss 门和单向跳落时必须单独成步；
4. 涉及钥匙消耗、Boss 战、任务推进或世界纪元切换时，必须显示执行前状态和执行后影响；
5. 目标在当前状态不可达时，返回最小阻断集，例如“目标只存在于玛利喀斯前的王城；当前为灰烬王城”，不得伪造绕路；
6. 若存在多条路线，至少区分推荐路线、低风险路线和不使用快速旅行路线，且说明权衡；
7. 全成就/补档模式按目标依赖关系排序，优先提示会永久错过或会触发世界纪元变化的目标。

### 10.7 路线说明顺序

每一步先说入口地标和楼层变化，再说目标名称：

1. 先告诉用户应找哪个门、井、断桥、排水口、梯子、屋顶或升降梯；
2. 明确“上楼/下楼/切换地下图/单向跳落”；
3. 再给 Boss、物品或赐福名称；
4. 在重叠楼层中显示相对高度和当前层；
5. 不能只给罗盘方向，因为复杂建筑内方向不如地标可靠。

## 11. 产品功能规格

### 11.1 地图与浏览

- 顶层切换：交界地地表 / 交界地地下 / 幽影之地 / 独立区域；
- 地区、map ID、地牢、楼层和世界纪元切换；互斥纪元不得同时显示为可走；
- 2D 正交图为默认；复杂区域提供可选爆炸层/3D 预览；
- 鼠标、触控、键盘和手柄友好；
- 图标聚类只改变显示，不改变真实坐标；
- URL 可保存地图、层、楼层、世界纪元、坐标、缩放、筛选和路线；
- 路线跨楼层或跨地图时，地图与步骤列表同步突出当前入口、当前层和下一连接。

### 11.2 搜索和点位

- 简中、繁中、英文、日文、内部 ID 和别名搜索；
- 分类、地区、DLC、世界状态、已收集、证据等级和剧透筛选；
- 点位卡片显示官方名、类型、精确层、入口、获取条件、奖励、版本、来源和置信度；
- 对不在当前楼层的点显示“上/下方”，而不是叠在同一点；
- 点位可以直接设为路线起点或终点，但普通 POI 不因此成为路线中继节点；
- 搜索结果必须同时给出“坐标距离”和“当前状态是否可达”，不能用前者替代后者。

### 11.3 路线与状态面板

- 用户可以检查和修改当前 `StateSnapshot`，并看见每一项来自存档、手动选择、游戏文件推导还是未知；
- 提供“当前位置直达”“从已激活赐福出发”“全成就/补档顺序”三种路线入口；
- 路线结果固定显示起始赐福、入口地标、楼层、必经连接、条件、单向性、状态变化和证据；
- 不可达结果显示阻断原因、所缺状态和仍可执行的最近替代目标；
- 用户切换世界纪元、钥匙数量、Boss/任务状态或路线 profile 后，路线必须立即重新验证；
- 技巧路线与漏洞路线使用明显不同的视觉和风险提示，默认关闭。

### 11.4 进度与存档

- 首版必须支持纯本地手动勾选；
- 可增加只读存档解析和连续存档轮询，所有解析在用户本机/浏览器完成；
- 存档数据转换为版本化 `StateSnapshot`，供 Phase 5 路线内核使用；路线内核本身不得依赖某一种存档解析器；
- 不写回存档、不上传存档、不连接在线游戏内存；
- 自动状态和用户手动覆盖必须分开保存，避免误判；
- 剧情分支不能用单一“完成”布尔值抹平；
- 解析器无法确认的 flag 必须保留为 unknown，不能自动猜成 false。

### 11.5 实时位置与实机轨迹

- 实时位置是可选输入适配器，不是可达性图的前置条件；它只更新 `RouteRequest.start`；
- 正式产品优先采用人工点选、只读存档轮询或其他不注入游戏的本地方式；
- 如为 QA 评估进程内坐标/轨迹采集，只能在隔离、离线、关闭 EAC 的测试环境进行，不随默认公开版分发；
- 任何在线/EAC 会话禁止注入、读写内存或启用传送功能；
- 实机轨迹可以生成 `TransitionCandidate` 和路线几何证据，但不得未经地图/楼层/方向/状态复核自动晋升为正式通行边；
- 位置落在错误楼层、地图边界外或未知连通分量时，界面必须显示“无法安全定位”，不能吸附到二维最近点。

### 11.6 离线与公开部署

- 本地包可完全离线运行；
- 在线版的核心地图和搜索应在网络不稳定时可缓存；
- 原始游戏资源与用户存档不得进入遥测；
- 页面明确声明非官方、目标版本、版权归属和各第三方许可证。

## 12. 分阶段执行与退出门

### Phase 0：版本、许可和环境冻结

**工作**

- 找到合法游戏安装；
- 记录标题画面版本；
- 建立关键文件 SHA-256 manifest；
- 固定 Smithbox、SoulsFormatsNEXT、Paramdex 等提交；
- 固定并审计 Compass、FrontierNav 文档、`er-guide`、ER Route Tracker 等当前对标版本、许可证和数据来源；
- 建立 source/license ledger；
- 建立 `data/online-source-registry.json`，逐源登记直接接入字段、禁用资产、许可证、revision/commit 和同步方式；
- 固定 `data/execution-policy.json`，把在线同步、游戏并行和本地归档三类任务变成可校验的调度边界；
- 为每个外部来源标明“事实真值 / 提取参考 / 语义参考 / 轨迹证据 / UX 对标”，禁止角色混用；
- 初始化 Git，隔离当前 demo；
- 确定本地版与公开版资源边界。

**退出门**

- 能从 manifest 唯一重现数据源；
- 不存在来源不明的工具或资源；
- 没有任何第三方地图、知识图谱或路线指南被误登记为 `local_game_verified`；第三方坐标必须保留原坐标系和证据等级，语义关系不能直接成为通行边；
- 原始游戏资产不在源码提交范围。

### Phase 1A：Online-first 真实可用 V1

**工作**

- 冻结 `data/online-source-registry.json` 中各来源的许可证、revision/commit、允许字段和禁止用途；
- 以小型文本/JSON 请求获取 Eldenpedia、ERDB 和经审计开源仓库的不可变 Source Snapshot，不镜像专有地图瓦片、图片或私有接口；
- 建立统一 `Entity`、`Placement`、`RouteLeg`、`SemanticRelation`、`SourceEvidence` 和来源坐标契约；
- 首先导入全成就依赖、关键错过项、全部赐福、地区/子地区、主要入口与关键 `WorldEpoch`；
- 使用 `er-guide` 的 grace-to-grace 路段结构和 Wiki walkthrough 路线语义建立 `RouteLeg` 候选，但排除来源混杂且未经独立确认的瓦片、图片和坐标；
- 每条默认路线边要求两个独立在线来源一致，或一个结构化来源加人工核对；只描述目标位置而未描述通行机制的资料不得生成路线边；
- 在线坐标保留来源坐标系，经版本化控制点变换投影到自制/抽象底图；不得冒充游戏原始 X/Y/Z；
- 接入现有 WebUI，并确保正式构建不读取 `data/graph.json` 演示点。

**退出门**

- 可发布 `Online Verified V1`，而不是演示数据或未经说明的“1:1 完成版”；
- 全部成就依赖至少具备来源、最近可用赐福、入口地标、楼层/位面、必要状态和可执行步骤；未知项显式返回 unknown；
- 地表、地下、独立地图/楼层和幽影之地四类顶层空间均可检索与导航，关键错过项和“黄金树大教堂 → 古兰桑克斯的雷电”通过金标准用例；
- 每条正式记录均具有来源 URL、revision/commit、许可证、获取时间、坐标类型和证据等级；
- `SemanticRelation` 进入路线编译器即构建失败；没有证据的临近点不自动连边；
- 发布物不含未经许可的瓦片、图片、批量专有坐标、游戏原始资产或演示数据。

### Phase 1B：四类地图本地真值样片

选取四个代表区：

1. 宁姆格福开放世界瓦片；
2. 希芙拉河地下连续区；
3. 史东薇尔多层独立地图；
4. 幽影城/保藏库 DLC 高垂直地图。

**工作**

- 解析 map IDs、MSB、坐标、文本、赐福和核心实体；
- 拼接/生成底图；
- 建立局部到显示坐标变换；
- 生成楼层；
- 建立 RouteNode 分类、SemanticRelation 隔离和 TransitionCandidate 晋升流程；
- 完成 20 条真实跨层 Transition；
- 完成至少 10 个“语义有关但不能通行”的负样本；
- 用 Smithbox 与实际离线游戏逐点核对。

**退出门**

- 四种地图均能从同一流水线生成；
- 坐标闭环达标；
- 楼层和入口没有人工假摆点；
- SemanticRelation 无法被样片寻路器读取；
- 门、电梯、梯子、跳落和传送的两端均为独立 RouteNode，方向正确；
- 任何一类失败都先修流水线，不扩展全图。

### Phase 2：全地图清单和统一坐标

**工作**

- 枚举所有 MSB/世界瓦片；
- 分类 playable/cutscene/unused/duplicate/unknown；
- 建立所有地图变换、父区域和连接；
- 建立完整 WorldEpoch 清单，至少明确玛利喀斯前王城/玛利喀斯后灰烬王城及其互斥地图、实体和边；
- 建立需要单独建模的机关状态清单，例如升降梯端点、门、传送激活和水位；
- 与 Souls Modding map overview 和 Smithbox map list 做差异报告。

**退出门**

- 100% 原始地图条目有分类和理由；
- 所有 playable 地图都有可打开的规范记录；
- 所有改变几何或可达性的持久状态都有 WorldEpoch 或明确排除理由；
- `unknown` 中不存在用户可进入但未建模的地图。

### Phase 3：全位面底图与楼层

**工作**

- 生成地表、地下、幽影之地瓦片金字塔；
- 为每个独立地图生成碰撞/Navmesh 平面图；
- 完成楼层聚合、遮罩和跨层箭头；
- 对重点垂直区域进行人工校图。

**退出门**

- 每个 playable map 至少有一个有效视图；
- 每个可到达楼层可单独选择；
- 不同楼层 POI 不再视觉重叠；
- 重点区域视觉抽查无严重错层。

### Phase 4：全量 POI 和状态

**工作**

- 完整解析 ItemLot、MSB、FMG、EMEVD、ESD 与相关 params；
- 生成唯一拾取、Boss、NPC、商店、基础设施和传送点；
- 分离 SemanticRelation、空间显示关系和 TransitionCandidate；
- 去重、排除 dummy/cut/phantom；
- 与 wiki.gg、Map For Goblins、Compass、FrontierNav 和主流互动地图做差异队列；
- 完成简中本地化与多语言别名。

**退出门**

- 100% 候选记录已发布或带理由排除；
- 关键类别不存在未解释差异；
- 所有展示点都有来源、版本和楼层；
- 任何语义或第三方关系都没有被自动转换为正式通行边；
- 不存在社区来源直接覆盖游戏坐标的记录。

### Phase 5：真实可达性图

**工作**

- 生成物理连通分量；
- 建立完整 RouteNode 清单和所有跨地图/跨楼层端点；
- 建立门、梯子、升降梯、气流、落差、传送等显式 Transition；
- 建立剧情、钥匙、机关、世界纪元和快速旅行 guard；
- 建立开门、消耗钥匙、启动机关、击败 Boss 和纪元切换等 effect；
- 实现版本化 RouteRequest/StateSnapshot/RouteResult 契约；
- 在 `(RouteNode, StateSnapshot)` 上验证状态随动作演化的路线规划；
- 生成路线几何、入口地标和逐步说明；
- 校准步行、骑马和机关时间；
- 建立无漏洞默认 profile、禁快速旅行 profile 和隔离的技巧/漏洞 profile。

**退出门**

- 所有跨地图和跨层连接都有证据；
- 每种 Transition 类型、每种 guard 原子及其真/假分支都有测试；
- 至少 120 条覆盖全部顶层位面和高风险区域的金标准路线全部实机通过；
- 至少 60 条不可达、反向不可行、世界纪元互斥或状态不足场景不会被错误规划；
- “黄金树大教堂赐福 → 古兰桑克斯的雷电”必须分别验证玛利喀斯前可执行路线与灰烬王城状态下不可获取结论；
- 任何 FrontierNav/Wiki/POI 语义关系都不能单独让两个 RouteNode 连通；
- 不存在“为了让图连通而补一条边”的数据。

### Phase 6：正式互动界面

**工作**

- 接入真实 tile、POI、楼层、状态和路线；
- 完成搜索、筛选、点位卡片、状态面板、路线 profile、阻断原因、分享链接和移动端；
- 实现“当前位置/已激活赐福 → 入口地标 → 楼层 → 必经连接 → 条件边 → 目标”的固定路线展示；
- 加入版本、来源和证据显示；
- 性能优化和离线包。

**退出门**

- 桌面和移动端核心流程通过；
- 5000+ 可见候选点时仍能平滑筛选/缩放；
- 首屏、搜索和层切换满足性能指标；
- 用户改变世界纪元、钥匙、Boss/任务状态后，路线和不可达原因同步更新；
- 当前 demo 数据在生产构建中为 0 条。

### Phase 7：只读进度系统

**工作**

- 手动清单；
- 对当前 Compass 实现和其他解析器做版本、许可证、正确性 spike；
- 可选本地只读存档解析与连续轮询适配器；
- event flag、物品、赐福、周目和世界纪元到 StateSnapshot 的映射；
- 分支和 NG+ 状态处理；
- 可选离线位置/实机轨迹 QA 适配器，生产默认不注入游戏；
- 隐私测试。

**退出门**

- 存档不离开用户设备；
- 不写回存档；
- 不在在线/EAC 会话读取或注入游戏进程，不提供传送功能；
- 已完成/未完成抽样与游戏一致；
- 同一存档生成的 StateSnapshot 与实机路线锁定/开放状态一致；
- 不能确认的状态明确标为未知。

### Phase 8：版本重基线与发布

**工作**

- 2026-08-28 之后重新提取；
- 生成 1.16.1 到实际新版本的全量 diff；
- 重新跑地图、POI、路线、视觉和许可门；
- 产出发布说明与已知限制。

**退出门**

- 页面显示真实目标版本；
- Severity 1/2 数据问题为 0；
- 授权/自制视觉路径明确；
- 所有第三方归属和许可随包发布。

## 13. 验证和验收标准

### 13.1 数据自动检查

- 所有外键、map ID、entity ID、lot ID、FMG ID 和 event flag 可解析；
- 原始坐标到显示坐标再反算，误差不高于 `0.001` 游戏单位；
- 任何 playable map 没有底图/楼层时构建失败；
- 任何正式 POI 缺来源、版本或楼层时构建失败；
- 所有 SemanticRelation 必须为 `routeable=false`，路由索引引用语义关系时构建失败；
- 每条 Transition 的起终点必须是有效 RouteNode，并拥有 map、floor、方向、guard、effect 和证据；
- 同层路线几何必须位于对应可行走包络内；跨墙、跨虚空或跨 floor 只能通过已声明的跳跃/落下/梯子/升降梯/传送等 Transition；
- 单向边自动生成反向边、互斥 WorldEpoch 同时生效、未知 guard 被当成 true 时构建失败；
- guard/effect 引用的 flag、物品、钥匙、机关和纪元必须可解析且类型正确；
- 重复唯一物、孤立 Transition、未解释的不可达赐福、循环剧情条件和不可满足状态触发失败；
- 当前坐标只能吸附到同 map、同 floor、同连通分量；跨层最近点吸附测试必须失败；
- 工具或 Param DataVersion 漂移触发失败；
- 简中名称为空时生成报告，不静默发布空文本。

### 13.2 视觉抽查

至少覆盖以下高风险区域：

- 史东薇尔城；
- 魔法学院；
- 王城与灰烬王城；
- 地下大道；
- 火山官邸；
- 法姆·亚兹拉；
- 米凯拉的圣树/艾布雷菲尔；
- 希芙拉河/诺克隆；
- 安瑟尔河/诺克史黛拉/腐败湖；
- 幽影城、保藏库和教区；
- 贝瑞特/艾尼尔·伊利姆；
- 劳弗古遗迹；
- 石棺大洞和深渊森林。

每区检查入口、最高层、最低层、至少 10 个 POI 和所有主要跨层点。

### 13.3 实机金标准

- 至少 300 个跨区域、跨类别点位实机截图/录像对照；
- 所有主线 Boss、赐福、地图碎片、关键物品和 DLC 成长物 100% 核对；
- 所有跨地图和跨楼层 Transition 100% 具有游戏版本、方向、条件和实机证据；
- 至少 120 条端到端可达路线与 60 条不可达反例；固定数量只是最低线，仍必须满足区域、连接类型和条件覆盖；
- 每个 guard 原子至少在 true/false 状态各验证一次；AND/OR/NOT 和 effect 链必须覆盖有效及无效分支；
- 玛利喀斯前王城/灰烬王城、已解锁/未解锁升降梯、封印气流、钥匙数量不足、未激活传送、Boss 门和单向落差必须进入反例集；
- “黄金树大教堂赐福 → 古兰桑克斯的雷电”作为强制状态对：玛利喀斯前验证可执行步骤，灰烬王城验证明确不可获取；
- 至少 20 个“同一坐标附近但入口位于其他楼层/远处”的用例，必须规划真实入口而不是二维直线；
- 至少 20 个“知识图谱有关联但玩家不能直接通行”的用例，必须保持不可达；
- 发现一个严重错点时，对同一生成规则的全部记录回归，不只修单点。

### 13.4 性能目标

- 中等桌面设备冷启动首个可交互地图不高于 3 秒；
- 已缓存层切换不高于 150 毫秒；
- 本地搜索 P95 不高于 100 毫秒；
- 平移/缩放目标 60 FPS，低端移动设备不得长期低于 30 FPS；
- 只加载视口内瓦片和可见类别，不一次性把全世界高分辨率资源塞入内存。

### 13.5 完成声明规则

以下措辞必须严格区分：

- “已提取”：只是流水线有输出；
- “已接入”：前端可以显示；
- “已交叉校验”：至少一个独立来源一致；
- “已实机验证”：有对应游戏版本的实际证据；
- “完整”：地图审计和候选实体均已分类，且无阻塞差异；
- “可达性完整”：所有可游玩地图/楼层的入口和跨层/跨图连接已建模，全部正式边有证据，全部条件类型有正反用例，且 Severity 1/2 错误路线为 0；
- “状态感知路线”：RouteResult 明确绑定 DatasetVersion、StateSnapshot 和 route profile，状态变化会重新规划；
- “1:1”：空间、数据、拓扑与可达性均通过，视觉许可状态另行注明。

## 14. 每阶段必须交付的证据产物

- `source-manifest`：版本、路径、哈希和工具提交；
- `license-ledger`：每个工具、页面、文本和视觉资产的许可；
- `map-inventory`：全部地图 ID、分类、父区域、状态和包含理由；
- `world-state-inventory`：WorldEpoch、玩家进度、机关状态、互斥关系和未知项；
- `transform-report`：坐标变换、控制点和闭环误差；
- `floor-inventory`：每张图的全部楼层与连接；
- `poi-audit`：候选、发布、排除、去重和差异统计；
- `relation-audit`：SemanticRelation、空间显示关系、TransitionCandidate 和正式 Transition 的隔离结果；
- `transition-audit`：所有通行边、条件、方向和证据；
- `state-effect-audit`：钥匙消耗、机关变化、Boss/任务推进和世界纪元切换；
- `source-discrepancies`：游戏与各在线地图/Wiki 的逐项差异；
- `route-contract-corpus`：RouteRequest、StateSnapshot、RouteResult、可达路线和不可达最小阻断集；
- `golden-routes`：全部金标准路线、反例、录像和版本索引；
- `visual-qa`：重点区域截图/录像索引；
- `release-report`：目标版本、覆盖范围、已知限制和未解决项。

这些报告比“页面看起来像完成了”更重要；没有报告就不能证明完整性。

## 15. 工期与资源估算

以下是达到本规格而不是再做一个演示页面的保守估算：

| 里程碑 | 累计工期参考 | 结果 |
|---|---:|---|
| V1-A 在线来源冻结 | 1–2 天 | 来源注册表、许可证、revision/commit 和首批 Source Snapshot 可复现 |
| V1-B 全成就导航 | 3–7 天 | 全成就依赖、关键错过项、赐福起点、入口/楼层、世界状态和步骤可用 |
| V1-C 世界导航骨架 | 1–2 周 | 地表、地下、独立地图/楼层、幽影之地的主要入口和跨区连接可用 |
| V1-D 收集扩展 | 2–4 周 | 主要唯一物、Boss、NPC、升级材料和地点逐类扩展并保留证据等级 |
| R0 本地真值样片 | 2–3 周（可与 V1-C/V1-D 错峰） | 四类地图、游戏坐标、楼层、关系隔离和状态路线样片通过 |
| R1 全世界底图 + 核心 POI | 6–10 周 | 地表/地下/DLC、主要唯一物和世界纪元骨架可用 |
| R2 全独立地图/全楼层 + 全量 POI | 14–22 周 | 所有 playable map、候选点、楼层和跨图端点完成审计 |
| R3 全状态可达性、路线、存档和发布 QA | 24–36 周 | 达到本文的完整拓扑、状态路线和 1:1 验收口径 |

上述工期适用于至少一名逆向/数据工程师、一名前端/地图工程师和一至两名持续进行实机路线 QA 的人员，并假定现有解析工具大部分可复用。若只有一名主工程师和一名非全职 QA，完整范围应按约 9–15 个月规划。自动化和 Codex 可以缩短解析、差异和测试工作，但不能取消版权审批、路线取证和实机复核。若只做“全图 + 点位”而不做全楼层和全状态路线，工期可明显缩短，但不得宣称完成本规格。

## 16. 主要风险

| 风险 | 后果 | 缓解措施 |
|---|---|---|
| 原始地图美术版权 | 公开版下架或侵权风险 | 本地版与公开版分轨；书面授权或自制底图 |
| 8 月 28 日新包/补丁 | 数据立即过期 | 版本 manifest、自动 diff、发布前重基线 |
| 游戏文件含 dummy/cut 数据 | 幽灵点和虚假完整率 | 候选分类、事件链、实机抽样和 exclusion ledger |
| Navmesh 不等于玩家通行 | 路线穿墙、漏跳跃或错判不可达 | 碰撞/Navmesh + 显式 Transition + 实机金标准 |
| 复杂世界状态 | 王城、NPC、Boss 和门状态混叠 | 状态变体和条件表达式，不压成单一布尔值 |
| 知识关系泄漏进导航图 | 因“属于同一区域”或“地图有关联”生成虚假捷径 | SemanticRelation 硬隔离、编译失败门和负样本 |
| 状态空间组合爆炸 | 钥匙、任务、Boss、机关组合导致规划过慢或漏解 | 分层状态、支配剪枝、不可逆纪元分区和基准用例 |
| 第三方站点 403/改版/无 API | 自动同步中断 | 多来源、revision 快照、人工审计，不依赖单站 |
| 第三方工具快速演进或资料陈旧 | 把已实现工具写成 stub，或依赖未验证新功能 | 固定提交、每阶段重审 README/测试/许可证；先做独立 spike |
| 实时位置/注入工具与 EAC 风险 | 封号、安全或分发风险 | 生产默认不注入；只读存档优先；进程采集仅离线隔离 QA |
| 楼层按高度粗切 | 地牢上下层错乱 | 连通分量、跨层点和人工校图 |
| 数千点位性能 | 页面卡顿 | WebGL、瓦片、视口加载、聚类和分层索引 |

## 17. 开工顺序

实际安装路径和基础哈希已经确认；从现在起按在线 V1 与本地校准双轨开工：

1. 在游戏运行期间按 `ONLINE_FIRST_V1_PLAN.md` 和 `data/execution-policy.json` 冻结在线来源、许可证与请求限额；
2. 建立小型 Source Snapshot，先接入全成就依赖、赐福、地区、入口、楼层、关键世界状态和唯一物；
3. 定义并冻结 `SemanticRelation`、`RouteLeg`、`RouteNode`、`Transition`、`StateSnapshot`、`RouteRequest` 和 `RouteResult` 契约；
4. 用两源规则建立“最近可用赐福 → 入口地标 → 楼层 → 必经连接 → 条件边 → 目标”，禁止按二维距离自动连边；
5. 以玛利喀斯前/后王城状态对和“黄金树大教堂 → 古兰桑克斯的雷电”为强制用例，交付 `Online Verified V1`；
6. 游戏退出后再执行本地工具 spike；有合格独立健康盘时制作只读快照，否则只在空闲窗口直接读取归档；
7. 先做宁姆格福、希芙拉河、史东薇尔、幽影城四类本地真值样片，把在线记录逐项升级为 `local_game_verified`；
8. 提交 transform、floor、POI、relation、transition、state-effect、来源差异和实机验收报告；
9. V1 达标后继续扩大在线覆盖，本地样片达标后继续全图 1:1 校准；二者任一未过门都不能冒充最终完成。

## 18. 本文核对过的主要在线依据

以下在线产品与仓库的公开功能基线最后核对于 2026-08-17。它们变化快，Phase 0 和每次发布重基线都必须重新检查当前提交、公开功能、许可证和数据新鲜度，不能把本表永久当成现状。

- [Bandai Namco：Elden Ring Patch Notes Version 1.16.1](https://en.bandainamcoent.eu/elden-ring/news/elden-ring-patch-notes-version-1161)
- [Bandai Namco：Tarnished Pack 内容与 2026-08-28 发布信息](https://en.bandainamcoent.eu/elden-ring/news/how-get-new-elden-ring-classes-weapons-and-torrent-skin-customizations)
- [Bandai Namco：Shadow of the Erdtree 官方页面](https://en.bandainamcoent.eu/elden-ring/elden-ring/shadow-of-the-erdtree)
- [Steam：ELDEN RING EULA](https://store.steampowered.com/eula/1245620_eula_0)
- [Smithbox](https://github.com/vawser/Smithbox)
- [Smithbox releases](https://github.com/vawser/Smithbox/releases)
- [Souls Modding：Map Overview](https://soulsmodding.com/doku.php?id=er-refmat%3Amap-overview)
- [Souls Modding：Using the World Map](https://soulsmodding.com/doku.php?id=tutorial%3Asmithbox-er-world-map)
- [SoulsFormatsNEXT](https://github.com/soulsmods/SoulsFormatsNEXT)
- [Paramdex](https://github.com/soulsmods/Paramdex)
- [Eldenpedia：Maps](https://eldenring.wiki.gg/wiki/Maps)
- [Eldenpedia：Editing Guidelines](https://eldenring.wiki.gg/wiki/Elden_Ring_Wiki%3AEditing_Guidelines)
- [Eldenpedia：许可说明](https://eldenring.wiki.gg/wiki/Category%3AEldenpedia)
- [MediaWiki Action API](https://www.mediawiki.org/wiki/API:Main_page)
- [Map For Goblins DLL](https://github.com/Jovial-Nik/ERR-MapForGoblins-DLL)
- [ERDB](https://github.com/EldenRingDatabase/erdb)
- [Elden Ring Fan API](https://github.com/deliton/eldenring-api)
- [Elden Ring Compass](https://github.com/EthanShoeDev/elden-ring-compass)
- [FrontierNav：Data Model](https://docs.frontiernav.net/editing-data/data-model)
- [FrontierNav：Deep Query](https://docs.frontiernav.net/navigation/query)
- [FrontierNav：Edit Interactive Maps](https://docs.frontiernav.net/editing-data/editing-interactive-maps)
- [FrontierNav：Terms of Use](https://docs.frontiernav.net/policies/terms-of-use)
- [FrontierNav：《艾尔登法环》公开关系示例](https://frontiernav.net/wiki/elden-ring/entities/atlus-plateau)
- [Elden Ring 100% Route Guide](https://github.com/aether-auto/er-guide)
- [ER Route Tracker](https://www.nexusmods.com/eldenring/mods/9294?tab=docs)
- [Wand：《艾尔登法环》地图](https://wand.com/maps/elden-ring)
- [AtlasForge：《艾尔登法环》地图](https://atlasforge.gg/elden-ring/map/lands_between)
- [MapGenie：《艾尔登法环》App Store 功能页](https://apps.apple.com/us/app/mapgenie-elden-ring-map/id1618843254)
- [Elden Ring Map Assembly: Technical Details](https://www.nexusmods.com/eldenring/articles/79)
- [deck.gl OrthographicView](https://deck.gl/docs/api-reference/core/orthographic-view)
- [deck.gl TileLayer](https://deck.gl/docs/api-reference/geo-layers/tile-layer)

---

**最终原则：凡是不能追溯到目标游戏版本、不能解释坐标变换、不能说明楼层、不能区分语义关系与通行边、不能给出方向/条件/状态效果/实机证据或不能合法发布的内容，都不能靠“看起来差不多”进入正式地图。坐标相近不等于能到，知识图谱有关联不等于能走，实时位置不等于会导航。**
